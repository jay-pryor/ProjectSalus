task_id: I-16
timestamp: 2026-05-16T00:00:00Z
title: "PDF report section-criticality policy — hard-fail required sections, audit-log optional failures"

quality_gates:
  G1_lint: pass            # ruff check src/ tests/ — "All checks passed!"
  G2_format: pass          # ruff format --check src/ tests/ — "85 files already formatted"
  G3_import: pass          # ruff check --select I src/ — "All checks passed!"
  G4_type: pass            # mypy src/salus/ — 37 errors, all pre-existing import-untyped; 0 introduced by I-16. Per CLAUDE.md informational.
  G5_security: pass        # bandit -r src/ -ll -q — Total issues: Low 3 (pre-existing), Medium 0, High 0.
  G6_unit_test: pass       # pytest tests/ --cov=src/salus --cov-fail-under=80 -q — 1366 passed, 4 skipped, 0 failed (896.75 s). tests/test_report_pdf.py — 38 passed (TestSectionCriticality class).
  G7_coverage: pass        # Total coverage 83.01%, above the 80% threshold. report/pdf.py 95%.
  G8_secrets: pass         # detect-secrets scan src/ — 0 files with secrets.
  coverage_pct: 83

review:
  l1_standards:
    testing: pass          # tests/test_report_pdf.py TestSectionCriticality — required-map / gap-map raise, optional-chart records, per-layer continue, required-template raise, optional-template-not-found records, clean-render empty list; additions only, no existing test modified
    error_handling: pass   # _render_to_b64 / _render_gap_map_to_b64 raise ReportRenderError chained with `from`, temp file cleaned in finally; optional chart/template failures append to ReportData.section_failures rather than silently dropping; required-section template errors raise
    logging: pass          # _log.warning for every omitted optional section (pdf.py and the /api/report handler); CLI writes a multi-line "degraded sections" block to stderr
    security: pass         # no new file I/O, path handling, or user input; change is internal report-assembly control flow
    interface_modules: N/A # no .js / .html / manifest.json files changed
  l2_agents:
    silent_failure_hunter: {status: pass, findings: 0}      # 1 observation (redundant `except ReportRenderError` in cli.py) self-withdrawn — the explicit clause is mandated by I-16 AC 8; 1 observation on the raise-vs-return policy shift confirmed intended
    spec_compliance: {status: pass, findings: 2}            # 13/13 acceptance criteria PASS; 2 non-blocking warnings — AC9 single-%s log format vs spec two-%s (functionally equivalent), AC10 4 of 7 tests renamed more descriptively
    type_design_analyzer: {status: pass, findings: 0}       # ReportRenderError, section_failures field, and the str|None -> str return-type tightening all confirmed sound
    regression_reviewer: {status: pass, findings: 1}        # D-589 — /api/report now returns 500 (was degraded 200) for a broken required template; intended per spec, but unexercised — regression test added
    module_architecture_reviewer: N/A                       # no JS/HTML/manifest files changed

defect_ids:
  - D-496   # O-5 — PDF renders blank sections silently (broad-except in render helpers)
  - D-502   # O-12 — PDF template syntax errors silently drop entire sections
  - D-589   # L2 — /api/report required-template-failure 500 path untested

notes:
  - Source — Triage bug-triage-2026-05-11 findings O-5 + O-12 (Group 3, HIGH/MEDIUM).
  - Policy — REQUIRED sections (cover, executive_summary, site_overview, coverage_analysis, gap_analysis, assumptions, appendix_sensors) and their mandatory map assets (composite_map, gap_map) raise ReportRenderError on failure. OPTIONAL sections (threat_analysis, kill_chain, saturation) and per-layer maps append to ReportData.section_failures and are omitted. The CLI surfaces section_failures to stderr; /api/report logs each.
  - AC9 log format — implementation logs the whole "<section>: <reason>" entry with a single %s rather than the spec's literal two-%s string; the omitted-section identity is fully present. Accepted as functionally equivalent.
  - Out of scope (per spec) — migrating sections to the I-12 modular contract; per-section red-stamp rendering.
  - Committed together with I-14 (commit 2) — shared cli.py / interface_api/app.py hunks. See I-14 gate-proof note.

overall: pass
