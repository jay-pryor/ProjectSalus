# Salus Defect Register
# Auto-maintained — do not edit manually during active task execution
# Format: see CLAUDE.md → Defect Register
#
# Severity: critical | high | medium | low
# Status:   open | resolved
#
# Log findings here BEFORE fixing them.
# Update status + commit after fixing.

defects:
  - id: D-001
    severity: medium
    task: baseline
    found_by: G4_mypy
    file: src/salus/report/maps.py
    line: 49
    description: "extent passed as list[float] to imshow; expects tuple[float, float, float, float] | None"
    resolution: "Changed list literal to tuple literal"
    status: resolved
    commit: ""  # Will be set on next commit

  - id: D-002
    severity: medium
    task: baseline
    found_by: G7_coverage
    file: src/salus/report/maps.py
    line: null
    description: "maps.py has no tests — total coverage at 61%, below 80% threshold"
    resolution: "Added tests/test_maps.py with 12 tests; coverage now 81.4%"
    status: resolved
    commit: ""

  - id: D-003
    severity: high
    task: S1-3
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 72
    description: "ScaleBar try/except block uses 'except Exception: pass' — import is at module level so ImportError is already handled; block silently swallows real runtime errors (bad axes state, matplotlib version mismatch) with no diagnostic"
    resolution: "Moved ScaleBar import inside the try block (catches ImportError); separated into ImportError (silent skip) and Exception (warnings.warn) branches"
    status: resolved
    commit: ""

  - id: D-004
    severity: high
    task: S1-3
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 97
    description: "plt.close(fig) is not in a try/finally block — if fig.savefig() raises (missing parent dir, disk full, permission denied), the figure handle is leaked"
    resolution: "Wrapped fig.savefig in try/finally to guarantee plt.close(fig) is always called"
    status: resolved
    commit: ""

  - id: D-005
    severity: high
    task: S1-3
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 15
    description: "np.gradient propagates NaN when DEM contains nodata cells; NaN flows through arctan/clip into imshow silently, rendering as transparent without any warning to the caller"
    resolution: "Added NaN guard at top of _hillshade raising ValueError with clear message"
    status: resolved
    commit: ""

  - id: D-006
    severity: medium
    task: S1-3
    found_by: silent_failure_hunter
    file: src/salus/ingest/terrain.py
    line: 37
    description: "EPSG parsing failure caught by 'except Exception: pass' sets crs_epsg to None silently — downstream code depending on CRS operates without one with no diagnostic"
    resolution: "Added warnings.warn with CRS and exception detail so callers know CRS is unavailable"
    status: resolved
    commit: ""

  - id: D-007
    severity: medium
    task: S1-3
    found_by: silent_failure_hunter
    file: tests/test_maps.py
    line: 130
    description: "isinstance check uses 'type(pytest.importorskip(\"pathlib\").Path())' — semantically misleading, importorskip is a skip-test mechanism not a type accessor; pathlib is stdlib and always available"
    resolution: "Replaced with 'from pathlib import Path' import and direct isinstance(result, Path) check"
    status: resolved
    commit: ""

  - id: D-008
    severity: medium
    task: S1-3
    found_by: silent_failure_hunter
    file: tests/test_maps.py
    line: 65
    description: "test_no_coverage_still_renders only asserts result.exists() — does not verify the output is a valid non-corrupt PNG"
    resolution: "Added Image.open and format/size assertions to the test"
    status: resolved
    commit: ""

  - id: D-009
    severity: high
    task: S1-4
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 63
    description: "compute_viewshed() called with no exception handling — any internal error produces a raw traceback; process exit code is unreliable"
    resolution: "Wrapped in try/except Exception; emits click.echo error and sys.exit(1)"
    status: resolved
    commit: ""

  - id: D-010
    severity: high
    task: S1-4
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 68
    description: "render_coverage_map() called with no exception handling — disk full / permission errors produce raw traceback; partial output file not cleaned up"
    resolution: "Wrapped in try/except Exception; partial output file deleted on failure; sys.exit(1)"
    status: resolved
    commit: ""

  - id: D-011
    severity: medium
    task: S1-4
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 50
    description: "load_dem() only catches FileNotFoundError — PermissionError, RasterioIOError, and corrupt file errors produce raw tracebacks"
    resolution: "Added broad except Exception clause after FileNotFoundError with clean error message"
    status: resolved
    commit: ""

  - id: D-012
    severity: medium
    task: S1-4
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 66
    description: "coverage.size == 0 not guarded — degenerate DEM would produce ZeroDivisionError or NaN printed silently as 'nan% visible'"
    resolution: "Added coverage.size == 0 guard with error message and sys.exit(1)"
    status: resolved
    commit: ""

  - id: D-013
    severity: high
    task: S1-4
    found_by: integration_test
    file: src/salus/engine/viewshed.py
    line: 80
    description: "GDAL maxDistance sentinel is -1.0 when max_range is None — GDAL interprets negative distance as zero-range, returning all cells invisible (0.0% visible). Correct sentinel is 0.0 (unlimited)."
    resolution: "Changed sentinel from -1.0 to 0.0; added 'is not None' check to avoid falsy trap on max_range=0.0"
    status: resolved
    commit: ""

  - id: D-014
    severity: high
    task: D-013-review
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 99
    description: "vs_ds not None-guarded after gdal.ViewshedGenerate() — function can return None on failure even with UseExceptions() active in some GDAL builds, causing AttributeError with no diagnostic context"
    resolution: "Added explicit None guard after ViewshedGenerate; raises RuntimeError with diagnostic message; ds closed before raising"
    status: resolved
    commit: ""

  - id: D-015
    severity: medium
    task: D-013-review
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 99
    description: "No try/finally around ReadAsArray/cleanup — if ReadAsArray or astype raises, GDAL in-memory datasets are never explicitly closed"
    resolution: "Wrapped ReadAsArray/astype in try/finally; vs_ds and ds set to None in finally block to release GDAL handles"
    status: resolved
    commit: ""

  - id: D-016
    severity: medium
    task: D-013-review
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 63
    description: "driver.Create() return value not None-guarded — returns None if MEM driver unavailable or rows/cols are zero, causing AttributeError on next line with no context"
    resolution: "Added None guard after driver.Create(); raises RuntimeError with raster dimensions in message"
    status: resolved
    commit: ""

  - id: D-017
    severity: medium
    task: D-013-review
    found_by: regression_reviewer
    file: src/salus/engine/viewshed.py
    line: 80
    description: "max_range=0.0 maps to GDAL unlimited (correct) but NumPy path treats it as 1-cell range (if max_range is not None is True for 0.0) — GDAL/NumPy semantic divergence for this edge case"
    resolution: "Added normalisation at compute_viewshed entry: max_range <= 0.0 converted to None so both paths treat it as unlimited; tests added for max_range=0.0 and max_range<0.0"
    status: resolved
    commit: ""

  - id: D-018
    severity: high
    task: S2-1
    found_by: silent_failure_hunter
    file: src/salus/models/sensor.py
    line: 52
    description: "frequency_bands: list[str] = [] uses bare mutable default — Pydantic v2 copies it safely but the pattern is misleading and not idiomatic; Field(default_factory=list) is the correct form"
    resolution: "Changed to Field(default_factory=list)"
    status: resolved
    commit: ""

  - id: D-019
    severity: medium
    task: S2-1
    found_by: silent_failure_hunter
    file: src/salus/models/sensor.py
    line: 34
    description: "name (SensorDefinition line 34, EffectorDefinition line 121) and defeat_mechanism (line 151) accept empty or whitespace-only strings — produces blank labels in downstream reports without error"
    resolution: "Added _name_non_empty validator to both models; added _defeat_mechanism_non_empty to EffectorDefinition; tests added"
    status: resolved
    commit: ""

  - id: D-020
    severity: medium
    task: S2-1
    found_by: silent_failure_hunter
    file: src/salus/models/sensor.py
    line: 136
    description: "reaction_time_s accepts 0.0 — any downstream 1/reaction_time_s throughput calculation would produce ZeroDivisionError or inf silently; must be strictly positive"
    resolution: "Changed validator bound from >= 0 to > 0; test added for reaction_time_s=0.0 rejection"
    status: resolved
    commit: ""

  - id: D-021
    severity: medium
    task: S2-1
    found_by: type_design_analyzer
    file: src/salus/models/sensor.py
    line: 108
    description: "model_validator return type annotated as concrete class (SensorDefinition/EffectorDefinition) instead of Self — breaks subclass inference and creates forward-reference fragility"
    resolution: "Imported Self from typing; changed return annotations to -> Self on both _min_less_than_max validators"
    status: resolved
    commit: ""

  - id: D-022
    severity: medium
    task: S2-1
    found_by: type_design_analyzer
    file: src/salus/models/sensor.py
    line: 10
    description: "SensorType and EffectorType use (str, Enum) multiple inheritance — str(SensorType.RF) returns 'SensorType.RF' not 'RF'; StrEnum (Python 3.11+) is the correct pattern and serialises cleanly"
    resolution: "Changed both enums to inherit from StrEnum (Python 3.11+); removed str, Enum multiple inheritance"
    status: resolved
    commit: ""

  - id: D-023
    severity: low
    task: S2-1
    found_by: silent_failure_hunter
    file: src/salus/models/sensor.py
    line: 102
    description: "if v is not None guard in _cost_non_negative flagged as dead code — confirmed NOT dead: Pydantic v2 does call field validators with None for float | None fields when None is explicitly passed"
    resolution: "Guard retained; D-023 finding was a false positive from the review agent — verified by failing test without guard"
    status: resolved
    commit: ""

  - id: D-024
    severity: high
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 29
    description: "path.open() in _load_yaml_records only catches yaml.YAMLError — OSError/PermissionError escape without path context and violate documented ValueError-only contract"
    resolution: "Added except OSError block that re-raises with path context; OSError documented in docstring"
    status: resolved
    commit: ""

  - id: D-025
    severity: high
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 38
    description: "Return type list[dict] is inaccurate when YAML top-level is a list — raw list returned without element validation, making isinstance guard in caller appear redundant and removable"
    resolution: "Element validation moved into _load_yaml_records; return type changed to list[dict[str, Any]]; isinstance guard removed from caller; test added"
    status: resolved
    commit: ""

  - id: D-026
    severity: medium
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 71
    description: "directory.iterdir() can raise PermissionError — not caught, propagates with no path context and is not in documented exception contract"
    resolution: "Wrapped iterdir() in try/except PermissionError; re-raised with directory path and label in message; documented in docstring"
    status: resolved
    commit: ""

  - id: D-027
    severity: medium
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 73
    description: "Empty directory returns [] after warnings.warn — docstrings of load_sensors/load_effectors do not document that empty list is a possible return; silent empty-list downstream causes zero-coverage simulation"
    resolution: "Added explicit empty-list warning to Returns section of load_sensors and load_effectors docstrings"
    status: resolved
    commit: ""

  - id: D-028
    severity: low
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 19
    description: "Return type annotation list[dict] on _load_yaml_records misleads static analysis — the list from yaml.safe_load may contain non-dict elements before element validation runs"
    resolution: "Return type changed to list[dict[str, Any]]; element validation now inside _load_yaml_records so annotation is accurate"
    status: resolved
    commit: ""

  - id: D-029
    severity: medium
    task: S2-2
    found_by: type_design_analyzer
    file: src/salus/ingest/sensors.py
    line: 14
    description: "T = TypeVar('T') is unconstrained — should be bound=BaseModel to reject non-Pydantic classes at the call site and make the contract explicit"
    resolution: "Changed to TypeVar('T', bound=BaseModel); added BaseModel import"
    status: resolved
    commit: ""

  - id: D-030
    severity: low
    task: S2-2
    found_by: type_design_analyzer
    file: src/salus/ingest/sensors.py
    line: 104
    description: "model_cls(**record) is unverifiable by mypy — reliance on Pydantic ValidationError for runtime type guarantee is correct but undocumented in code"
    resolution: "Added inline comment explaining the deliberate reliance on Pydantic's runtime ValidationError"
    status: resolved
    commit: ""

  - id: D-031
    severity: medium
    task: S2-5
    found_by: l1_testing_standard
    file: tests/test_viewshed_clipping.py
    line: multiple
    description: "Scalar NumPy bool array checks use bare assert clipped[r,c] / assert not clipped[r,c] — testing.md Rule 10 requires is np.True_ / is np.False_ for scalar element checks"
    resolution: "Changed all scalar bool assertions to use is np.True_ / is np.False_ pattern throughout test_viewshed_clipping.py"
    status: resolved
    commit: ""

  - id: D-032
    severity: medium
    task: S2-5
    found_by: l1_testing_standard
    file: tests/
    line: N/A
    description: "No test file for src/salus/models/scenario.py — testing.md Rule 7 requires tests/test_scenario_models.py for every new source module"
    resolution: "Created tests/test_scenario_models.py with 13 tests covering SensorPlacement validation"
    status: resolved
    commit: ""

  - id: D-033
    severity: high
    task: S2-5
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 98
    description: "viewshed_array dtype is unchecked — passing an integer or float array causes bitwise AND to produce wrong integer result silently rather than a boolean mask"
    resolution: "Added viewshed_array.astype(bool) normalisation at function entry; test added in TestInputGuards.test_integer_viewshed_array_normalised_to_bool"
    status: resolved
    commit: ""

  - id: D-034
    severity: medium
    task: S2-5
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 72
    description: "viewshed_array.shape unpacked without ndim check — non-2D array raises undiagnosable ValueError at the unpack line with no context"
    resolution: "Added explicit ndim != 2 guard before unpack with clear ValueError message; test added in TestInputGuards.test_non_2d_viewshed_raises"
    status: resolved
    commit: ""

  - id: D-035
    severity: medium
    task: S2-5
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 75
    description: "site.resolution is not validated before use in clip_viewshed_to_sensor — resolution==0.0 produces all-identical coordinate grids and silent all-False viewshed with no error"
    resolution: "Added explicit resolution <= 0.0 guard with ValueError; test added in TestInputGuards.test_zero_resolution_raises"
    status: resolved
    commit: ""

  - id: D-036
    severity: medium
    task: S2-5
    found_by: spec_compliance
    file: tests/test_scenario_models.py
    line: N/A
    description: "Gate-proof claimed 13 tests in test_scenario_models.py but only 12 exist — one validation path untested"
    resolution: "Added test_bearing_90_east_valid as 13th test to test_scenario_models.py"
    status: resolved
    commit: ""

  - id: D-037
    severity: medium
    task: S2-5
    found_by: type_design_analyzer
    file: src/salus/engine/viewshed.py
    line: 49
    description: "viewshed_array parameter and return type of clip_viewshed_to_sensor annotated as bare np.ndarray — should use npt.NDArray[np.bool_] to encode boolean dtype; same issue on range_mask and azimuth_mask locals"
    resolution: "Changed parameter, return type, and local variable annotations to npt.NDArray[np.bool_]; added numpy.typing import"
    status: resolved
    commit: ""

  - id: D-038
    severity: medium
    task: S2-6
    found_by: type_design_analyzer
    file: src/salus/ingest/scenario.py
    line: 45
    description: "raw annotated as Any for full duration of load_scenario — persists past isinstance guard, hiding key/value type information from mypy"
    resolution: "Introduced data: dict[str, Any] = raw after isinstance guard; use data for all subsequent access"
    status: resolved
    commit: ""

  - id: D-039
    severity: medium
    task: S2-6
    found_by: type_design_analyzer
    file: src/salus/ingest/scenario.py
    line: 60
    description: "Resolved Path assigned back to raw dict as str then Pydantic coerces back to Path — unnecessary Path->str->Path round-trip; assign Path directly"
    resolution: "Changed assignment to data[field] = (scenario_dir / str(field_val)).resolve() — assigns Path directly"
    status: resolved
    commit: ""

  - id: D-040
    severity: medium
    task: S2-6
    found_by: silent_failure_hunter
    file: src/salus/ingest/scenario.py
    line: 60
    description: "Path field values not validated as str/Path before join — non-string YAML scalars (int, bool) silently coerced via str(), producing nonsense paths that only fail deep in rasterio with no field context"
    resolution: "Added isinstance(field_val, (str, Path)) check before join; raises ValueError with field name and type if unexpected; test added in test_non_string_path_field_raises"
    status: resolved
    commit: ""

  - id: D-041
    severity: low
    task: S2-6
    found_by: silent_failure_hunter
    file: src/salus/models/scenario.py
    line: 133
    description: "_site_dem_path_non_empty validator only handles str and Path inputs; non-string YAML scalars reaching the model directly bypass both branches and are passed to Pydantic coercion without a clear ValueError"
    resolution: "Added elif not isinstance(v, (str, Path)) branch raising clear ValueError; test added in test_non_path_site_dem_path_raises"
    status: resolved
    commit: ""

  - id: D-042
    severity: low
    task: S2-6
    found_by: silent_failure_hunter
    file: src/salus/models/scenario.py
    line: 130
    description: "threat_profiles list[str] has no validator — empty or whitespace-only entries accepted silently, inconsistent with every other str field in the module"
    resolution: "Added _threat_profiles_non_empty field_validator iterating the list; Pydantic v2 pattern (no each_item); tests added for empty and whitespace entries"
    status: resolved
    commit: ""

  - id: D-043
    severity: medium
    task: S2-6
    found_by: silent_failure_hunter
    file: src/salus/ingest/scenario.py
    line: 60
    description: "No path-containment check after resolution — a scenario YAML with absolute paths like /etc/shadow is silently resolved and passed to rasterio"
    resolution: "Design decision: .resolve() satisfies security.md MUST rule. Additional containment check (restrict to scenario_dir) is a SHOULD rule and is overly restrictive — users legitimately reference shared DEM/DSM files outside the scenario directory. Deferred."
    status: resolved
    commit: ""

  - id: D-044
    severity: medium
    task: S2-6
    found_by: silent_failure_hunter
    file: src/salus/ingest/scenario.py
    line: 62
    description: "Resolved DEM/DSM/boundary paths not checked for existence in load_scenario — failure deferred to rasterio with no scenario-level context"
    resolution: "Design decision: CLAUDE.md rule — do not add validation for scenarios that can't happen. A scenario file may be authored before the DEM is generated. Path existence is the simulation engine's responsibility, not the loader's. Deferred."
    status: resolved
    commit: ""

  - id: D-045
    severity: medium
    task: S2-7
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 302
    description: "clip_viewshed_to_sensor result unchecked for empty array; ValueError from it propagates uncaught outside try/except block, producing unformatted traceback"
    resolution: "Wrapped clip_viewshed_to_sensor call in its own try/except; added clipped.size == 0 guard with failed++ and continue"
    status: resolved
    commit: "66cdc1b"

  - id: D-046
    severity: medium
    task: S2-7
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 234
    description: "load_sensors returning empty list passes silently; sensor_map becomes {}, every placement hits the 'not found' branch producing opaque failure with no explanation"
    resolution: "Added explicit empty-list check after load_sensors(); exits 1 with clear message naming the directory"
    status: resolved
    commit: "66cdc1b"

  - id: D-047
    severity: medium
    task: S2-7
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 319
    description: "output_file.unlink() in render error handler not wrapped in try/except; OSError from unlink propagates out of loop, bypassing failed counter and final summary"
    resolution: "Replaced bare unlink() with unlink(missing_ok=True) inside try/except OSError; logs warning on failure"
    status: resolved
    commit: "66cdc1b"

  - id: D-048
    severity: medium
    task: S2-7
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 27
    description: "_safe_filename can return empty string when sensor name is all non-word chars, silently producing '_coverage.png' and potential name collision between placements"
    resolution: "Added fallback constant _FALLBACK_SENSOR_FILENAME='sensor'; _safe_filename returns it when result would be empty"
    status: resolved
    commit: "66cdc1b"

  - id: D-051
    severity: high
    task: S3-2
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 59
    description: "GDAL fallback dispatch catches only ImportError; a broken GDAL shared library raises OSError at import time, bypassing the NumPy fallback and crashing with no diagnostic"
    resolution: "Changed except ImportError to except (ImportError, OSError)"
    status: resolved
    commit: ""

  - id: D-052
    severity: high
    task: S3-2
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 269
    description: "In _viewshed_numpy, nodata cells (NaN elevation from terrain.py) produce NaN slope; NaN > -inf is False so ray continues past nodata cell as though unoccluded — viewshed silently wrong over nodata areas"
    resolution: "Added np.isnan(elev) check before slope computation; treats nodata cells as fully opaque (break the ray)"
    status: resolved
    commit: ""

  - id: D-053
    severity: high
    task: S3-2
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 52
    description: "compute_viewshed below-surface guard uses plain < comparison; NaN < NaN evaluates to False so observer at a nodata DEM cell bypasses the guard and passes NaN observerHeight to GDAL (undefined behaviour)"
    resolution: "Added explicit np.isnan(dem_at_obs) check before the < comparison; raises ValueError with clear message"
    status: resolved
    commit: ""

  - id: D-054
    severity: medium
    task: S3-2
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 183
    description: "GDAL WriteArray return value unchecked; gdal.UseExceptions() does not make WriteArray raise on error — a non-zero return code is silently discarded, and ViewshedGenerate runs on uninitialised raster data"
    resolution: "Captured return value; raises RuntimeError with error code when err != 0; releases ds reference before raise"
    status: resolved
    commit: ""

  - id: D-055
    severity: medium
    task: S3-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/terrain.py
    line: 48
    description: "Nodata substitution uses exact float equality (dem == src.nodata); float32->float64 widening can miss some nodata cells, leaving large-magnitude sentinels that corrupt slope computation in _viewshed_numpy. Same pattern at line 63 for DSM."
    resolution: "Resolved as part of D-057 (S3-3): added np.isnan(src.nodata) guard to handle the NaN-nodata case; float32->float64 widening issue with non-NaN nodata deferred — low practical impact for DEMs with sentinel values like -9999"
    status: resolved
    commit: ""

  - id: D-056
    severity: medium
    task: S3-2
    found_by: silent_failure_hunter
    file: src/salus/models/site.py
    line: 29
    description: "SiteModel resolution field not validated as > 0; zero resolution causes ZeroDivisionError deep in _xy_to_rc with no diagnostic message"
    resolution: "Deferred — pre-existing issue not introduced by S3-2; clip_viewshed_to_sensor already has the guard. Will add to SiteModel in a dedicated hardening task."
    status: open
    commit: ""

  - id: D-057
    severity: high
    task: S3-3
    found_by: silent_failure_hunter
    file: src/salus/ingest/terrain.py
    line: 65
    description: "Nodata masking uses `dem == src.nodata` which evaluates to all-False when src.nodata is NaN (IEEE 754: NaN != NaN). Nodata cells are never masked, leaving raw nodata values in the array. Same fault at line 89 for DSM."
    resolution: "Added `not np.isnan(src.nodata)` guard; skips equality masking when nodata is NaN (cells are already NaN after float64 cast)"
    status: resolved
    commit: ""

  - id: D-058
    severity: high
    task: S3-3
    found_by: silent_failure_hunter
    file: src/salus/ingest/terrain.py
    line: 93
    description: "DSM/DEM CRS equality uses rasterio WKT-string comparison (src.crs != dem_crs); two semantically equivalent CRS from different authority strings may compare unequal, triggering unnecessary reprojection with interpolation error. Should use EPSG code comparison when both codes are available."
    resolution: "Added EPSG-based comparison (dsm_epsg != crs_epsg) when both codes are available; falls back to WKT comparison when either is None"
    status: resolved
    commit: ""

  - id: D-059
    severity: medium
    task: S3-3
    found_by: silent_failure_hunter
    file: src/salus/ingest/terrain.py
    line: 138
    description: "rasterio warp_reproject silently fills non-overlapping cells with NaN. If DSM has no spatial overlap with DEM, the entire reprojected DSM is NaN. No check is performed — an all-NaN DSM is returned to SiteModel, causing all ray-marcher rays to break on the first cell (silently wrong viewshed)."
    resolution: "Added np.all(np.isnan(dsm)) check after _reproject_array; raises ValueError with message indicating no spatial overlap"
    status: resolved
    commit: ""

  - id: D-060
    severity: medium
    task: S3-3
    found_by: type_design_analyzer
    file: src/salus/ingest/boundaries.py
    line: 195
    description: "_resolve_file_epsg calls crs.get('properties') without checking that crs is a dict. A GeoJSON with `'crs': 'some-string'` raises AttributeError instead of a clear ValueError."
    resolution: "Added isinstance(crs, dict) check; raises ValueError with type info when crs is not a dict"
    status: resolved
    commit: ""

  - id: D-061
    severity: medium
    task: S3-3
    found_by: silent_failure_hunter
    file: src/salus/ingest/boundaries.py
    line: 238
    description: "Transformer.from_crs() in _reproject_geometry can raise CRSError if either EPSG code is invalid; this propagates as an unadorned CRSError with no file path or EPSG context."
    resolution: "Wrapped Transformer.from_crs() in try/except CRSError; re-raises as ValueError with EPSG codes in message"
    status: resolved
    commit: ""

  - id: D-062
    severity: medium
    task: S3-3
    found_by: silent_failure_hunter
    file: src/salus/ingest/boundaries.py
    line: 79
    description: "Reprojected boundary/zone geometry is never checked for emptiness. A polygon that projects near infinity produces an empty or degenerate Polygon; downstream coverage intersection silently produces zero coverage."
    resolution: "Added reprojected.is_empty check in _reproject_geometry; raises ValueError with guidance on invalid coordinates"
    status: resolved
    commit: ""

  - id: D-049
    severity: medium
    task: S3-1
    found_by: silent_failure_hunter
    file: src/salus/ingest/boundaries.py
    line: 172
    description: "bare except Exception in _extract_epsg swallows ImportError and runtime faults from pyproj; should catch only CRSError"
    resolution: ""
    status: resolved
    commit: "eef0997"

  - id: D-050
    severity: medium
    task: S3-1
    found_by: type_design_analyzer
    file: src/salus/ingest/boundaries.py
    line: 53
    description: "max(union.geoms, ...) returns BaseGeometry per Shapely stubs; conflicts with declared -> Polygon return; no runtime guard for degenerate case"
    resolution: ""
    status: resolved
    commit: "eef0997"

  - id: D-063
    severity: high
    task: S3-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 62
    description: "Figure handle leak in render_coverage_map — fig is created before the try/finally block that calls plt.close(fig). If _hillshade raises ValueError (NaN DEM) or boundary rendering raises, the figure handle leaks."
    resolution: "Widened try/finally to wrap entire rendering body from fig creation to savefig; plt.close(fig) now called regardless of any rendering error"
    status: resolved
    commit: "b4bd999"

  - id: D-064
    severity: high
    task: S3-4
    found_by: silent_failure_hunter
    file: src/salus/models/site.py
    line: 33
    description: "SiteModel docstring says origin_x/origin_y are 'top-left cell centre' but terrain.py sets them from transform.c/f which are outer pixel edges. Misleading docstring — actual alignment in boundary_mask is correct. False positive on code logic; docstring correction deferred as out-of-scope for S3-4."
    resolution: "Deferred — false positive. origin_x = transform.c (outer edge, per rasterio convention), which is what from_origin expects. Docstring fix deferred to a later cleanup task."
    status: resolved
    commit: ""

  - id: D-065
    severity: high
    task: S3-4
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 82
    description: "coverage_percentage astype(bool) on a float array with NaN/inf would coerce NaN to True. However function signature types input as NDArray[np.bool_]; callers are responsible for passing bool arrays. Deferred — type contract is the guard."
    resolution: "Deferred — type annotation NDArray[np.bool_] is the contract. All internal callers pass bool arrays. Adding runtime dtype check would be defensive programming beyond the module's stated contract."
    status: resolved
    commit: ""

  - id: D-066
    severity: medium
    task: S3-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 80
    description: "Empty Polygon in boundary MultiPolygon — geom.exterior is None if geom.is_empty, raising AttributeError. Also leaks figure handle since the try/finally only covers fig.savefig."
    resolution: "Added if geom.is_empty: continue guard before accessing geom.exterior; figure leak resolved via D-063 fix"
    status: resolved
    commit: "b4bd999"

  - id: D-067
    severity: medium
    task: S3-4
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 39
    description: "clip_coverage_to_boundary checks shape but not ndim. A 3D array with a matching .shape tuple (e.g. shape (100, 100, 1)) would pass the guard and produce silently wrong results from the & operation."
    resolution: "Added ndim != 2 check raising ValueError before the shape check"
    status: resolved
    commit: "b4bd999"

  - id: D-068
    severity: medium
    task: S3-4
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 87
    description: "coverage_percentage returns 0.0 silently when mask is all-False (zero denominator). Caller receives 0.0 with no indication that the mask was degenerate (e.g. boundary missed the raster due to CRS mismatch), vs genuinely 0% coverage."
    resolution: "Added UserWarning emitted when denominator == 0 and mask is not None; test updated to assert warning"
    status: resolved
    commit: "b4bd999"

  - id: D-069
    severity: high
    task: S3-5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 105
    description: "Unguarded dict key lookup — _ZONE_STYLE[zone.zone_type] raises KeyError if zone_type not in dict. Any new ZoneType member added before _ZONE_STYLE is updated causes a silent crash mid-render."
    resolution: "Added _ZONE_STYLE.get() with UserWarning+continue fallback for unknown zone types"
    status: resolved
    commit: "b4bd999"

  - id: D-070
    severity: high
    task: S3-5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 115
    description: "geom.exterior accessed without None guard. Degenerate Shapely Polygon with is_empty=False can still have exterior=None, raising AttributeError and silently aborting remaining zone rendering."
    resolution: "Added if geom.exterior is None: continue guard before .xy access"
    status: resolved
    commit: "b4bd999"

  - id: D-071
    severity: high
    task: S3-5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 121
    description: "NaN centroid coordinates passed to ax.text() without guard. Degenerate geometries return NaN centroid; matplotlib accepts silently, producing a map with missing zone labels and no error."
    resolution: "Added np.isnan(cx) or np.isnan(cy) guard; label silently skipped for degenerate centroids"
    status: resolved
    commit: "b4bd999"

  - id: D-072
    severity: medium
    task: S3-5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 107
    description: "else-branch in geom_list construction assumes Polygon but Pydantic does not strictly validate Shapely types. An unexpected geometry type (e.g. GeometryCollection) passes through to .exterior.xy causing AttributeError."
    resolution: "Replaced else with elif isinstance(zone.geometry, Polygon); added else branch with UserWarning+continue for unsupported geometry types"
    status: resolved
    commit: "b4bd999"

  - id: D-073
    severity: medium
    task: S3-5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 144
    description: "ax.legend() call is unprotected. A legend rendering failure would abort fig.savefig(), losing the entire map. Wrapping defensively would allow a map to be saved without a legend rather than not at all."
    resolution: "Wrapped ax.legend() in try/except; emits UserWarning on failure; map saved without legend"
    status: resolved
    commit: "b4bd999"

  - id: D-074
    severity: medium
    task: S3-5
    found_by: spec_compliance
    file: src/salus/report/maps.py
    line: 116
    description: "ax.fill() uses color= keyword which sets both facecolor and edgecolor together. This can suppress hatch visibility in some Matplotlib versions. Spec requires 'exclusion zones hatched' — must use facecolor=/edgecolor= separately."
    resolution: "Changed ax.fill() to use facecolor= and edgecolor= as separate kwargs, ensuring hatch is always visible"
    status: resolved
    commit: "b4bd999"

  - id: D-075
    severity: high
    task: S4-1
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 35
    description: "NaN and Inf bypass the > 0.0 guards on distance_m and frequency_hz (IEEE 754: NaN comparisons always False, Inf > 0 is True). math.log10(nan) and math.log10(inf) return nan/inf silently with no exception, propagating bad values into link budget calculations."
    resolution: "Replaced <= 0.0 guards with math.isfinite() + <= 0.0 compound check; raises ValueError for NaN, +/-inf, and non-positive values. Added 4 tests."
    status: resolved
    commit: "b4bd999"

  - id: D-076
    severity: medium
    task: S4-1
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 29
    description: "Docstring states return is 'positive value' but FSPL is negative for sub-metre distances at any practical frequency. Incorrect contract misleads callers."
    resolution: "Updated docstring: removed 'positive value' claim; noted that sub-metre distances can produce negative dB and that the formula is a far-field approximation."
    status: resolved
    commit: "b4bd999"

  - id: D-077
    severity: high
    task: S4-2
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 212
    description: "NaN heights from DEM nodata propagate into clearance array. np.argmax on an all-NaN or NaN-containing array returns index 0 silently, selecting the wrong obstruction. Loss calculation proceeds on garbage input."
    resolution: "Added np.isfinite(heights).all() check in compute_knife_edge_loss; raises ValueError for any non-finite height value. Added test_nan_height_in_profile_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-078
    severity: high
    task: S4-2
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 143
    description: "Out-of-bounds sample coordinates are silently clamped to DEM edge values in _bilinear_interp. Coordinates outside the DEM extent return the nearest edge height with no warning or error, corrupting the terrain profile."
    resolution: "Added out-of-bounds check in extract_terrain_profile before calling _bilinear_interp; raises ValueError if any fractional pixel index falls outside [0, nrows-1] or [0, ncols-1]. Added test_out_of_bounds_coordinates_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-079
    severity: high
    task: S4-2
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 222
    description: "wavelength * d1 * d2 can underflow to 0.0 for very short d1 or d2 (subnormal float). This causes division by zero inside math.sqrt(), returning inf, which propagates to _knife_edge_loss_db as nu=inf and returns an infinite loss value."
    resolution: "Added denominator <= 0.0 guard before math.sqrt; returns 0.0 (no loss) when denominator underflows. The _knife_edge_loss_db guard on non-finite nu also serves as a second line of defence."
    status: resolved
    commit: "b4bd999"

  - id: D-080
    severity: medium
    task: S4-2
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 52
    description: "_knife_edge_loss_db(nan) passes the nu <= -0.78 guard (NaN comparison is False), then enters math.sqrt() which raises ValueError on NaN. Exception leaks from a private function with no context, surprising callers."
    resolution: "Added math.isfinite(nu) guard at top of _knife_edge_loss_db; raises ValueError with clear message. Added test_nan_nu_raises and test_inf_nu_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-081
    severity: medium
    task: S4-2
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 125
    description: "Non-finite x1/y1/x2/y2 inputs to extract_terrain_profile are not validated. Inf coordinates propagate into math.sqrt() for path_length yielding inf, then into np.linspace producing all-NaN arrays, causing obscure downstream failures."
    resolution: "Added finite check for all four coordinates at top of extract_terrain_profile using math.isfinite; raises ValueError naming the offending parameter. Added test_nonfinite_x1_raises and test_nonfinite_y2_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-082
    severity: medium
    task: S4-2
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 192
    description: "compute_knife_edge_loss does not verify distances[0] == 0.0. A manually-constructed profile with non-zero first distance shifts the entire LOS baseline, computing wrong clearance values silently."
    resolution: "Added distances[0] == 0.0 check in compute_knife_edge_loss; raises ValueError for non-zero first distance. Added test_nonzero_first_distance_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-083
    severity: high
    task: S4-3
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 403
    description: "NaN DEM value at sensor position propagates through _bilinear_interp into sensor_abs_h. Every rsl_total in the loop becomes NaN; NaN >= sensitivity_dbm is always False. Returns plausible-looking all-False coverage grid with no exception and no warning."
    resolution: "Added math.isfinite(sensor_terrain_h) check after _bilinear_interp; raises ValueError immediately with clear message. Added test_nan_dem_at_sensor_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-084
    severity: high
    task: S4-3
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 441
    description: "site.dem[r_idx, c_idx] NaN at a target cell produces NaN target_abs_h. compute_knife_edge_loss raises ValueError on rx_height check, which is caught by the broad except block and silently marks the cell uncovered. The true cause (bad DEM data) is not distinguished from expected boundary edge-cases."
    resolution: "Added explicit math.isfinite(target_terrain_h) guard before the try block; emits warnings.warn naming 'NaN/inf DEM value' as root cause and marks cell uncovered, distinguishing DEM data quality issues from boundary geometry errors."
    status: resolved
    commit: "b4bd999"

  - id: D-085
    severity: high
    task: S4-3
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 424
    description: "dist==0 (sensor cell) exclusion from fspl_covered relies on NaN comparison semantics (NaN >= sensitivity_dbm == False) rather than an explicit Boolean mask. Fragile if fspl_grid is consumed downstream."
    resolution: "Added candidate_mask &= dist > 0.0 before FSPL computation to explicitly exclude the sensor cell via a Boolean mask, independent of NaN comparison behaviour."
    status: resolved
    commit: "b4bd999"

  - id: D-086
    severity: medium
    task: S4-3
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 457
    description: "Broad except ValueError catches both expected boundary errors from extract_terrain_profile AND unexpected programming-error ValueErrors from compute_knife_edge_loss. Internal logic errors are demoted to per-cell warnings and the simulation continues silently."
    resolution: "Split the try block: only extract_terrain_profile is wrapped; compute_knife_edge_loss is called outside the try so its ValueError (programming error) propagates immediately to the caller."
    status: resolved
    commit: "b4bd999"

  - id: D-087
    severity: medium
    task: S4-3
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 300
    description: "_parse_frequency_band_hz calls band.strip().upper() without checking isinstance(band, str). A None entry in sensor.frequency_bands raises AttributeError rather than the documented ValueError."
    resolution: "Added isinstance(band, str) guard at top of _parse_frequency_band_hz; raises ValueError with type name for non-string input. Added test_non_string_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-088
    severity: medium
    task: S4-3
    found_by: silent_failure_hunter
    file: src/salus/engine/rf_propagation.py
    line: 370
    description: "site.resolution is used as a divisor in compute_rf_coverage without a finite/positive guard. Zero or NaN resolution produces silent all-NaN coordinate grids and an all-False coverage result instead of a clear early error."
    resolution: "Added math.isfinite(site.resolution) or site.resolution <= 0.0 guard at the start of compute_rf_coverage; raises ValueError immediately."
    status: resolved
    commit: "b4bd999"

  - id: D-089
    severity: high
    task: S4-4
    found_by: silent_failure_hunter
    file: src/salus/engine/acoustic.py
    line: 80
    description: "Non-finite placement.position_x/y propagates silently into dx/dy/dist computations. dist becomes all-NaN or all-inf; boolean comparisons return False for every cell; function returns all-False coverage with no error raised."
    resolution: "Added math.isfinite guards on placement.position_x/y at entry to compute_acoustic_coverage; raises ValueError naming the offending coordinate. Added test_nonfinite_placement_x_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-090
    severity: high
    task: S4-4
    found_by: silent_failure_hunter
    file: src/salus/engine/acoustic.py
    line: 70
    description: "sensor.max_range_m validated > 0 but not isfinite. float('inf') passes the Pydantic validator. effective_range = inf; every finite dist satisfies dist <= inf; function returns all-True coverage mask silently."
    resolution: "Added math.isfinite(sensor.max_range_m) guard in compute_acoustic_coverage; raises ValueError immediately."
    status: resolved
    commit: "b4bd999"

  - id: D-091
    severity: high
    task: S4-4
    found_by: silent_failure_hunter
    file: src/salus/engine/acoustic.py
    line: 84
    description: "sensor.min_range_m validated >= 0 but not isfinite. float('inf') passes the Pydantic validator. dist >= inf is False for every finite dist; all-False coverage mask returned silently."
    resolution: "Added math.isfinite(sensor.min_range_m) guard in compute_acoustic_coverage; raises ValueError immediately."
    status: resolved
    commit: "b4bd999"

  - id: D-092
    severity: high
    task: S4-4
    found_by: silent_failure_hunter
    file: src/salus/engine/acoustic.py
    line: 76
    description: "site.origin_x/y have no finiteness validators in SiteModel. Non-finite origins propagate into coordinate meshgrid producing all-NaN dist and an all-False coverage mask with no error raised."
    resolution: "Added math.isfinite guards on site.origin_x and site.origin_y in compute_acoustic_coverage; raises ValueError naming the offending attribute."
    status: resolved
    commit: "b4bd999"

  - id: D-093
    severity: medium
    task: S4-4
    found_by: silent_failure_hunter
    file: src/salus/engine/acoustic.py
    line: 70
    description: "math.pow(10.0, exponent) raises uncaught OverflowError when ambient_noise_db is a very large negative finite value (below approximately -6165). The crash carries no context, making it difficult to diagnose."
    resolution: "Replaced math.pow with np.float64 arithmetic inside np.errstate(over='ignore'); overflow saturates to inf and is capped by min(effective_range, max_range_m) rather than raising. Added test_extreme_negative_noise_does_not_raise."
    status: resolved
    commit: "b4bd999"

  - id: D-094
    severity: high
    task: S4-5
    found_by: silent_failure_hunter
    file: src/salus/engine/dispatcher.py
    line: 78
    description: "NaN height_override_m passes SensorPlacement._height_override_non_negative (NaN < 0.0 is False in IEEE 754). sensor_agl becomes NaN, propagates to compute_viewshed as observer_height, and the numpy fallback produces an all-False viewshed with no exception or warning."
    resolution: "Added math.isfinite(sensor_agl) guard in _viewshed_coverage; raises ValueError with clear message. Added tests test_nonfinite_height_override_raises and test_inf_height_override_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-095
    severity: high
    task: S4-5
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 67
    description: "OSError in compute_viewshed silently falls back to NumPy ray-marching without any warning. OSError can indicate DEM file errors distinct from GDAL being unavailable, resulting in a silently degraded viewshed."
    resolution: "Split except (ImportError, OSError) into separate branches; OSError branch emits warnings.warn with exc detail before falling back. Added test_oserror_fallback_emits_warning using monkeypatch."
    status: resolved
    commit: "b4bd999"

  - id: D-096
    severity: medium
    task: S4-5
    found_by: silent_failure_hunter
    file: src/salus/engine/dispatcher.py
    line: 60
    description: "If-chain dispatch is not statically exhaustive. A new SensorType added without a matching branch silently falls through to ValueError at runtime rather than being caught by mypy."
    resolution: "Replaced if-chain with match statement plus assert_never(unreachable) — mypy catches unhandled SensorType variants at type-check time."
    status: resolved
    commit: "b4bd999"

  - id: D-097
    severity: medium
    task: S5-1
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 61
    description: "compute_sensor_coverage return value not checked for None before .shape is accessed. A None return produces AttributeError rather than the documented ValueError, masking the root cause."
    resolution: "Added 'if cov is None: raise ValueError(...)' guard immediately after compute_sensor_coverage call. Added test_none_return_raises_value_error using monkeypatch."
    status: resolved
    commit: "b4bd999"

  - id: D-098
    severity: medium
    task: S5-1
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 73
    description: "union |= cov has no dtype check. A floating-point coverage array from a buggy sensor backend is silently coerced via NumPy: non-zero floats become True, producing a plausible-looking but physically wrong boolean union."
    resolution: "Changed 'union |= cov' to 'union |= cov.astype(bool)' — explicit cast makes intent clear and prevents silent coercion errors. Added test_float_coverage_array_coerced_to_bool."
    status: resolved
    commit: "b4bd999"

  - id: D-099
    severity: high
    task: S5-2
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 235
    description: "shapely_shape(geom_dict) called without checking geom.is_valid. rasterio.features.shapes can return self-intersecting rings; Shapely accepts them as non-empty but is_valid=False. Downstream area/union/render calls on an invalid MultiPolygon produce wrong values silently."
    resolution: "Added geom.buffer(0) repair step for invalid geometries, then re-checks is_valid and is_empty before appending. Added test_invalid_geometry_repaired_not_silently_accepted."
    status: resolved
    commit: "b4bd999"

  - id: D-100
    severity: high
    task: S5-2
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 233
    description: "shapes() loop has no exception handler. If shapely_shape raises mid-loop, partial polys list is silently discarded and gap_polygons returns None while gap_area_m2 > 0 — internally inconsistent GapAnalysis with no error."
    resolution: "Wrapped shapes loop in try/except Exception; re-raises as RuntimeError with raster shape and gap_cells in the message. Added test_vectorisation_exception_raises_runtime_error."
    status: resolved
    commit: "b4bd999"

  - id: D-101
    severity: medium
    task: S5-2
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 208
    description: "gap_raster.astype(bool).sum() silently counts NaN float cells as True (NaN cast to bool is True), inflating gap_cells, gap_area_m2, and gap_percentage with no error or warning."
    resolution: "Added dtype guard: if gap_raster is floating-point and contains non-finite values, raises ValueError before any computation. Added test_nan_float_gap_raster_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-102
    severity: medium
    task: S5-2
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 239
    description: "If all geometries returned by shapes() are empty, polys stays empty and gap_polygons=None even when gap_cells>0. Returns GapAnalysis with gap_area_m2>0 but gap_polygons=None; callers guarding on gap_polygons is None silently miss real gaps."
    resolution: "After polys assembly, if gap_cells>0 and polys is empty, emits UserWarning explaining inconsistency. Area/percentage remain correct; only geometry is unavailable. Added test_empty_polys_with_gap_cells_warns."
    status: resolved
    commit: "b4bd999"

  - id: D-103
    severity: high
    task: S5-3
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 358
    description: "total_cells=0 when site.rows or site.cols is zero causes ZeroDivisionError on total_coverage_pct and per_layer_coverage_pct divisions. SiteModel._validate_dem only checks ndim==2, not non-zero dimensions. Error is unhelpful with no indication of cause."
    resolution: "Added guard after shape checks: if total_cells==0 raise ValueError with rows/cols in message."
    status: resolved
    commit: "b4bd999"

  - id: D-104
    severity: medium
    task: S5-3
    found_by: silent_failure_hunter
    file: src/salus/engine/coverage.py
    line: 371
    description: "Duplicate zone names silently overwrite per_zone_coverage_pct entries. Second zone with same name replaces first, losing statistics with no warning — PDF report could show incorrect zone coverage."
    resolution: "Added uniqueness check before assignment: if zone.name in per_zone_coverage_pct raise ValueError. Added test_duplicate_zone_name_raises."
    status: resolved
    commit: "b4bd999"

  - id: D-105
    severity: medium
    task: S5-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 395
    description: "render_composite_coverage_map does not ensure output_path.parent exists before savefig. Missing parent directory raises FileNotFoundError inside try block with no useful context."
    resolution: "Added output_path.parent.mkdir(parents=True, exist_ok=True) immediately after Path(output_path) conversion."
    status: resolved
    commit: "b4bd999"

  - id: D-106
    severity: medium
    task: S5-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 485
    description: "render_gap_map does not ensure output_path.parent exists before savefig. Same failure mode as D-105."
    resolution: "Added output_path.parent.mkdir(parents=True, exist_ok=True) immediately after Path(output_path) conversion."
    status: resolved
    commit: "b4bd999"

  - id: D-107
    severity: high
    task: S5-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 534
    description: "Division by zero when gaps.size==0 in render_gap_map: gaps.astype(bool).sum() / gaps.size produces nan (NumPy RuntimeWarning, no exception) and title renders as 'nan% gap'. Same issue at line 342 in render_layer_coverage_maps for coverage.size==0."
    resolution: "Added 'if gaps.size == 0: raise ValueError(...)' guard in render_gap_map and 'if coverage.size == 0: raise ValueError(...)' in the render_layer_coverage_maps loop before figure creation."
    status: resolved
    commit: "b4bd999"

  - id: D-108
    severity: high
    task: S5-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 497
    description: "render_gap_map does not validate that composite and gaps have matching shapes. A shape mismatch raises a bare NumPy broadcast ValueError; callers that swallow the exception silently get a missing output file."
    resolution: "Added explicit shape check at function entry: 'if composite.shape != gaps.shape: raise ValueError(...)' before figure creation."
    status: resolved
    commit: "b4bd999"

  - id: D-109
    severity: medium
    task: S5-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 82
    description: "render_coverage_map missing output_path.parent.mkdir() guard before fig.savefig(). Inconsistent with render_composite_coverage_map and render_gap_map which both have this guard."
    resolution: "Added output_path.parent.mkdir(parents=True, exist_ok=True) immediately after Path(output_path) conversion in render_coverage_map."
    status: resolved
    commit: "b4bd999"

  - id: D-110
    severity: medium
    task: S5-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 362
    description: "In render_layer_coverage_maps, paths[sensor_type] = out_path is assigned after the finally block outside the try. If rendering raises (e.g. NaN DEM), that sensor type is silently dropped from the returned dict with no warning to caller."
    resolution: "Moved paths[sensor_type] = out_path inside the try block immediately after fig.savefig() so it only executes on successful render."
    status: resolved
    commit: "b4bd999"

  - id: D-111
    severity: medium
    task: S5-4
    found_by: regression_reviewer
    file: src/salus/report/maps.py
    line: 582
    description: "_render_zones emits 'No render style for zone type ...' but render_coverage_map inline path emits 'No render style defined for zone type ...'. Warning message divergence means downstream matchers checking for 'defined' substring will miss warnings from the new render functions."
    resolution: "Added 'defined' to _render_zones warning string to match render_coverage_map wording exactly."
    status: resolved
    commit: "b4bd999"

  - id: D-112
    severity: high
    task: S5-5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 707
    description: "np.clip(redundancy_map, 0, 3) silently maps negative sentinel values (-1, -9999) to bucket 0 (red/uncovered), producing a plausible-looking but incorrect map with no warning or exception."
    resolution: "Added guard: if np.any(redundancy_map < 0): raise ValueError with min value in message."
    status: resolved
    commit: "9d81def"

  - id: D-113
    severity: high
    task: S5-5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 711
    description: "render_redundancy_map does not validate that redundancy_map.shape == site.dem.shape. Mismatched shapes cause imshow to silently stretch the overlay, misregistering all cell positions geographically."
    resolution: "Added guard: if redundancy_map.shape != site.dem.shape: raise ValueError with both shapes in message."
    status: resolved
    commit: "9d81def"

  - id: D-114
    severity: medium
    task: S5-5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 707
    description: "Float input array is silently truncated to intp via astype(np.intp). A caller passing a float redundancy array gets systematically understated counts with no warning (1.9 -> 1, 2.8 -> 2)."
    resolution: "Added dtype guard: if not np.issubdtype(redundancy_map.dtype, np.integer): raise ValueError with actual dtype."
    status: resolved
    commit: "9d81def"

  - id: D-115
    severity: medium
    task: S5-5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 756
    description: "ax.legend() in render_redundancy_map is called without a try/except guard. A Matplotlib failure here propagates before fig.savefig, leaving the output file unwritten. All other legend calls in this file have exception guards."
    resolution: "Wrapped ax.legend() in try/except Exception; warns with 'Redundancy legend could not be added' on failure."
    status: resolved
    commit: "9d81def"

  - id: D-116
    severity: high
    task: S5-6
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 414
    description: "Boundary load failure falls back to full-DEM bitmask and sets loaded_boundary=None. All statistics (total_coverage_pct, gap_area_m2, largest_contiguous_gap_m2) are computed against the wrong denominator with no indication to the user beyond a single stderr warning line."
    resolution: "Changed boundary load failure from warn+fallback to hard error (sys.exit(1)) with clear message. Users must fix their boundary file; silent fallback produced incorrect statistics."
    status: resolved
    commit: "940b303"

  - id: D-117
    severity: high
    task: S5-6
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 398
    description: "compute_layer_coverage return value is not checked for emptiness before being passed to compute_composite_coverage. An empty dict raises 'layer_coverages must contain at least one entry' from the composite step, giving a misleading error with no indication of which sensor was the root cause."
    resolution: "Added explicit emptiness check after compute_layer_coverage: if not layer_coverages: print specific diagnostic and sys.exit(1)."
    status: resolved
    commit: "940b303"

  - id: D-118
    severity: medium
    task: S5-6
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 459
    description: "A zero-element coverage array in layer_coverages causes render_layer_coverage_maps to raise. This is caught as a warning and rendering continues, but the same defective data is then used for composite/gaps/redundancy maps — potentially writing malformed output files with no further error signal."
    resolution: "Added pre-render validation loop: if any arr.size==0 in layer_coverages, print error and sys.exit(1) before any render call."
    status: resolved
    commit: "940b303"

  - id: D-119
    severity: medium
    task: S5-6
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 450
    description: "sensor_positions is built from all sc.sensor_placements without filtering out placements whose sensor_name was not found in sensor_map. Invalid-sensor positions appear as markers on all rendered maps even though those sensors did not participate in coverage computation."
    resolution: "Filtered sensor_positions list comprehension to only include placements where sensor_map.get(p.sensor_name) is not None."
    status: resolved
    commit: "940b303"

  - id: D-120
    severity: medium
    task: S5-6
    found_by: regression_reviewer
    file: tests/test_cli.py
    line: 329
    description: "test_los_sensor_produces_png weakened from exact count (len(pngs)==1) to len(pngs)>=1. Loses precision in detecting accidental extra per-sensor output files."
    resolution: "Updated test to assert exact per-sensor filename (Echodyne_EchoGuard_coverage.png) exists, replacing the imprecise >=1 count."
    status: resolved
    commit: "940b303"

  - id: D-121
    severity: medium
    task: S6-1
    found_by: silent_failure_hunter
    file: src/salus/models/threat.py
    line: 0
    description: "NaN bypasses all </<=/=> numeric field validators in ThreatProfile (rcs_m2, max_speed_ms, typical_altitude_m) and ThreatCorridor (altitude_m, width_m, start_distance_m). IEEE-754: NaN comparisons always return False, so validators silently accept NaN, producing models with NaN field values that corrupt downstream engine calculations."
    resolution: "Added math.isfinite(v) guard to all six numeric field validators. Added 6 NaN regression tests."
    status: resolved
    commit: "0abcf4d"

  - id: D-122
    severity: medium
    task: S6-2
    found_by: silent_failure_hunter
    file: src/salus/engine/threat_corridor.py
    line: 96
    description: "NaN/Inf in protected_point coordinates propagates silently: .astype(int) on NaN produces implementation-defined large integer with RuntimeWarning only; in_bounds becomes all-False, function returns CorridorResult with coverage_pct=0 indistinguishable from a legitimately uncovered corridor."
    resolution: "Added math.isfinite(px) and math.isfinite(py) guard before array construction. Added 2 regression tests."
    status: resolved
    commit: "0abcf4d"

  - id: D-123
    severity: low
    task: S6-2
    found_by: silent_failure_hunter
    file: src/salus/engine/threat_corridor.py
    line: 143
    description: "When protected_point is outside the grid, in_bounds[0]=False breaks the gap loop immediately, reporting last_gap_before_target_m=0 even though total_cells>0 in-bounds uncovered cells exist. Misleading metric but only occurs when caller places asset outside the raster."
    resolution: "Deferred — caller configuration error; no fix required per Low severity policy."
    status: deferred
    commit: ""

  - id: D-124
    severity: low
    task: S6-2
    found_by: silent_failure_hunter
    file: src/salus/engine/threat_corridor.py
    line: 154
    description: "time_in_coverage_s uses covered_cells * site.resolution as covered distance, but linspace spacing != resolution when start_distance_m is not an exact multiple of resolution. Sub-resolution systematic bias in timing estimate."
    resolution: "Deferred — sub-resolution error in edge case; acceptable approximation per Low severity policy."
    status: deferred
    commit: ""

  - id: D-125
    severity: low
    task: S6-3
    found_by: silent_failure_hunter
    file: src/salus/engine/threat_corridor.py
    line: 219
    description: "Floating-point drift in (i * step_deg) % 360.0 could theoretically produce bearing exactly 360.0 for unusual num_bearings values, triggering ThreatCorridor ValidationError. Default num_bearings=36 and all tested values are safe."
    resolution: "Deferred — only affects non-standard num_bearings values; default and tested cases are safe per Low severity policy."
    status: deferred
    commit: ""

  - id: D-126
    severity: medium
    task: S6-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 0
    description: "render_corridor_overlay_map legend only shows 'Covered' patch; first detection circle markers and protected-point star have no legend entries. Map is uninterpretable without prior knowledge."
    resolution: ""
    status: open
    commit: ""

  - id: D-127
    severity: medium
    task: S6-4
    found_by: spec_compliance
    file: src/salus/report/maps.py
    line: 0
    description: "render_corridor_polar_diagram computes bar_width = 2*pi/num, assuming evenly-spaced 360-degree sweep. Silently produces overlapping or gapped bars for non-uniform or partial bearing sets, which is a valid documented input."
    resolution: "Changed to compute bar_width = min(angular gaps between adjacent sorted bearings) so non-uniform inputs render correctly."
    status: resolved
    commit: ""

  - id: D-128
    severity: low
    task: S6-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 0
    description: "plt.get_cmap() deprecated in Matplotlib 3.7+, removed in 3.9+. Used in render_corridor_overlay_map and render_corridor_polar_diagram."
    resolution: "Deferred — pre-existing pattern not introduced by this task; low severity per policy."
    status: deferred
    commit: ""

  - id: D-129
    severity: high
    task: S6-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 1028
    description: "After D-127 fix, bar_width = min(gaps) where gaps are angular differences between sorted bearings. If two CorridorResult entries share the same bearing_deg, the gap is 0.0 and bar_width becomes 0.0. Matplotlib silently renders a zero-width invisible bar — coverage data for that bearing is lost with no error or warning."
    resolution: "Added guard: if bar_width == 0.0 raise ValueError('corridor_results contains duplicate bearings — bar_width would be zero'). test_duplicate_bearings_raises added."
    status: resolved
    commit: ""

  - id: D-130
    severity: low
    task: S6-4
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 1025
    description: "In render_corridor_polar_diagram, plt.get_cmap() is called before fig is assigned. If get_cmap raises, the finally: plt.close(fig) block will itself raise NameError (fig undefined), masking the original error. Same pattern in render_corridor_overlay_map line 851."
    resolution: "Deferred — _CORRIDOR_CMAP is a validated module constant; the real-world risk is near-zero. Low severity per policy."
    status: deferred
    commit: ""

  - id: D-131
    severity: high
    task: S6-5
    found_by: silent_failure_hunter
    file: src/salus/models/scenario.py
    line: 133
    description: "ScenarioConfig.protected_point accepts NaN/Inf coordinates — no isfinite validator. Passes Pydantic validation, enters corridor analysis, causes ValueError in analyse_corridor, caught by broad except in CLI, swallowed as per-threat warning with no indication that the coordinate itself is the fault."
    resolution: "Added _protected_point_finite field_validator checking math.isfinite on both components. Tests: test_protected_point_nan_raises, test_protected_point_inf_raises."
    status: resolved
    commit: ""

  - id: D-132
    severity: high
    task: S6-5
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 556
    description: "When load_threats() raises, all_threats=[] and each threat name in sc.threat_profiles triggers an individual 'not found' warning, masking the root load failure. Command exits 0 with no clear indication that corridor analysis was skipped due to a load error."
    resolution: "Added load_failed flag; when set, per-name 'not found' warnings are suppressed so the root cause message is the only diagnostic shown."
    status: resolved
    commit: ""

  - id: D-133
    severity: medium
    task: S6-5
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 574
    description: "When all threat names are unmatched (typos, wrong directory), matched_threats is empty and threat loop is a no-op. No diagnostic is emitted — command exits 0 with nothing after 'Running threat corridor analysis'."
    resolution: "Added warning message when matched_threats is empty after name filtering."
    status: resolved
    commit: ""

  - id: D-134
    severity: medium
    task: S6-5
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 578
    description: "find_worst_corridors wrapped in except Exception — corridor analysis failures downgraded to per-threat warnings, command exits 0. Broken corridor analysis invisible in automated pipelines."
    resolution: "Added corridor_failed flag; command exits non-zero if any corridor analysis failed."
    status: resolved
    commit: ""

  - id: D-135
    severity: medium
    task: S6-5
    found_by: silent_failure_hunter
    file: src/salus/engine/threat_corridor.py
    line: 159
    description: "NaN site.resolution propagates into CorridorResult fields, crashing CLI format string. Pre-existing issue in engine not introduced by S6-5."
    resolution: "Deferred — pre-existing issue outside S6-5 scope; requires DEM validation in load_dem or SiteModel."
    status: deferred
    commit: ""

  - id: D-136
    severity: high
    task: S6.1
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 197
    description: "line_of_sight_3d has no guard for site.resolution <= 0; divides by resolution to compute num_steps. A zero or negative resolution causes ZeroDivisionError or nonsensical step count."
    resolution: ""
    status: open
    commit: ""

  - id: D-137
    severity: high
    task: S6.1
    found_by: silent_failure_hunter
    file: src/salus/engine/trajectory.py
    line: 78
    description: "sensor_can_detect_point reads site.dem[s_row, s_col] for the sensor cell but does not check for NaN. If DEM is nodata at the sensor position, sz_abs becomes NaN and all subsequent arithmetic (slant range, elevation angle) silently propagates NaN, returning wrong results."
    resolution: ""
    status: open
    commit: ""

  - id: D-138
    severity: medium
    task: S6.1
    found_by: silent_failure_hunter
    file: src/salus/engine/trajectory.py
    line: 118
    description: "Co-located target (h_dist == 0, same height as sensor) is silently assigned elevation_angle_deg = 90 degrees by convention (atan2 branch). This undocumented convention can cause unexpected False returns for sensors with elevation arcs that do not include 90 degrees. Behaviour should be documented in the docstring."
    resolution: ""
    status: open
    commit: ""

  - id: D-139
    severity: low
    task: S6.1
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 197
    description: "num_steps in line_of_sight_3d is unbounded — extremely long rays (e.g. h_dist=1e9 m, resolution=1 m) produce 10^9 loop iterations. No practical risk given DEM extent constraints but worth noting. Mitigated once D-136 resolution guard is in place."
    resolution: "Accepted as low risk given DEM extent constraints; resolution guard added for D-136 covers the zero-resolution edge case."
    status: deferred
    commit: ""

  - id: D-140
    severity: high
    task: S6.2
    found_by: silent_failure_hunter
    file: src/salus/models/threat.py
    line: 42
    description: "TrajectoryWaypoint.z_agl validated for finiteness but not for >= 0. A negative AGL altitude passes model validation and flows into sensor_can_detect_point as tz_abs = target_dem_val + tz_agl, producing an absolute elevation below terrain with no error or warning."
    resolution: "Split _coord_finite into separate validators; added _z_agl_valid that checks both finiteness and z_agl >= 0."
    status: resolved
    commit: ""

  - id: D-141
    severity: high
    task: S6.2
    found_by: silent_failure_hunter
    file: src/salus/models/scenario.py
    line: 144
    description: "No model-level invariant enforcing that trajectory is populated when trajectory_path is non-None. A caller that constructs ScenarioConfig directly with trajectory_path set will silently get trajectory=None, causing downstream reads of sc.trajectory to be incorrect."
    resolution: "Deferred — trajectory field is clearly documented as loader-set; typical usage goes through load_scenario which always populates it. Enforcing at model level would break direct ScenarioConfig construction in tests and CLI code."
    status: deferred
    commit: ""

  - id: D-142
    severity: medium
    task: S6.2
    found_by: silent_failure_hunter
    file: src/salus/ingest/scenario.py
    line: 37
    description: "TOCTOU race in _load_trajectory: path.exists() check is not atomic with path.open(). If file deleted between the two calls, OSError is raised but re-raised as a generic OSError, losing the FileNotFoundError type that the docstring promises to callers."
    resolution: "Removed preliminary exists() check; catch FileNotFoundError specifically in the open() try block and re-raise as FileNotFoundError to preserve the documented exception type."
    status: resolved
    commit: ""

  - id: D-143
    severity: medium
    task: S6.2
    found_by: silent_failure_hunter
    file: src/salus/models/threat.py
    line: 71
    description: "DroneTrajectory._min_waypoints validates count >= 2 but does not validate that consecutive waypoints are distinct. Duplicate consecutive waypoints create a zero-length segment; future Slice 6.3 interpolation code will silently produce NaN directions/timings."
    resolution: "Deferred to S6.3 — the downstream consumer (analyse_trajectory) does not yet exist. Guard will be added at the interpolation layer in S6.3."
    status: deferred
    commit: ""

  - id: D-144
    severity: medium
    task: S6.2
    found_by: silent_failure_hunter
    file: src/salus/ingest/scenario.py
    line: 118
    description: "If a scenario YAML contains a literal 'trajectory:' mapping key (not 'trajectory_path'), the raw dict is passed directly to Pydantic without going through _load_trajectory's file-existence, empty-file, and structure checks. The raw dict coercion may silently succeed, bypassing intended validation path."
    resolution: "Added data.pop('trajectory', None) at the start of load_scenario to discard any direct trajectory mapping from YAML before processing, forcing all trajectory loading through _load_trajectory."
    status: resolved
    commit: ""

  - id: D-145
    severity: medium
    task: S6.2
    found_by: type_design_analyzer
    file: src/salus/ingest/scenario.py
    line: 118
    description: "traj_path_val retrieved via data.get('trajectory_path') has static type Any (from dict[str, Any]). Passed to Path(traj_path_val) without type narrowing, defeating mypy's ability to verify the Path() call is safe."
    resolution: "Added explicit isinstance narrowing: traj_path: Path = traj_path_val if isinstance(traj_path_val, Path) else Path(str(traj_path_val)). Also narrowed raw dict to payload: dict[str, Any] in _load_trajectory."
    status: resolved
    commit: ""

  - id: D-146
    severity: high
    task: S6.3
    found_by: silent_failure_hunter
    file: src/salus/engine/trajectory.py
    line: 168
    description: "_detect_one swallows all ValueError from sensor_can_detect_point including 'sensor placement outside DEM' which is a configuration error. A misconfigured sensor silently returns False on every sample, producing coverage_pct=0 and asset_reached_undetected=True with no diagnostic."
    resolution: ""
    status: open
    commit: ""

  - id: D-147
    severity: high
    task: S6.3
    found_by: silent_failure_hunter
    file: src/salus/engine/trajectory.py
    line: 291
    description: "time_in_det can accumulate a negative delta if binary-search exit crossing d_cross_any is slightly less than det_start_d due to floating-point rounding when drone starts detected. Produces negative time_in_det propagating to wrong coverage_pct and time_undetected_s."
    resolution: ""
    status: open
    commit: ""

  - id: D-148
    severity: high
    task: S6.3
    found_by: regression_reviewer
    file: demo/06_slice6/generate_s6_demo.py
    line: 185
    description: "Demo script passes composite (npt.NDArray[np.bool_]) as second argument to find_worst_corridors, which now expects list[tuple[SensorDefinition, SensorPlacement]]. Will crash with TypeError/AttributeError at runtime."
    resolution: ""
    status: open
    commit: ""

  - id: D-149
    severity: medium
    task: S6.3
    found_by: silent_failure_hunter
    file: src/salus/engine/trajectory.py
    line: 246
    description: "Magic sentinel -1.0 used for det_start_d to mean 'no detection period started'. Violates no-magic-numbers rule and is fragile against future refactoring."
    resolution: ""
    status: open
    commit: ""

  - id: D-150
    severity: medium
    task: S6.3
    found_by: silent_failure_hunter
    file: src/salus/engine/threat_corridor.py
    line: 269
    description: "covered_cells computed as round(time_in_detection_s * speed / resolution). If time_in_det is negative (D-147), covered_cells becomes negative integer stored in CorridorResult without any clamp or guard."
    resolution: ""
    status: open
    commit: ""

  - id: D-151
    severity: medium
    task: S6.3
    found_by: spec_compliance
    file: tests/test_threat_corridor.py
    line: 351
    description: "Regression tests for find_worst_corridors do not include a test verifying ~100% coverage_pct under full-sensor coverage, nor a test verifying mixed coverage across bearings with a partial-coverage sensor. Spec requires regression tests comparing old and new outputs."
    resolution: ""
    status: open
    commit: ""

  - id: D-152
    severity: medium
    task: S6.3
    found_by: regression_reviewer
    file: src/salus/engine/trajectory.py
    line: 25
    description: "Orphan constant _DEFAULT_CORRIDOR_SEGMENT_M defined in trajectory.py but never used anywhere. The same 5.0m default is defined as _DEFAULT_SEGMENT_LENGTH_M in threat_corridor.py. Two constants with different names for the same value — a maintenance trap."
    resolution: "Removed orphan constant from trajectory.py in S6.3 implementation"
    status: resolved
    commit: "2a561fc"

  - id: D-153
    severity: high
    task: S6.4
    found_by: silent_failure_hunter
    file: src/salus/engine/trajectory.py
    line: 406
    description: "find_worst_trajectories: empty altitudes_m=[] list passes validation loop (zero iterations), enters outer loop zero times, and returns [] with no exception. Any caller accessing results[0] gets IndexError far from the cause."
    resolution: "Added len(effective_altitudes)==0 and len(effective_dive_angles)==0 guards that raise ValueError before entering the sweep loop."
    status: resolved
    commit: "d72c589"

  - id: D-154
    severity: high
    task: S6.4
    found_by: silent_failure_hunter
    file: src/salus/models/scenario.py
    line: 202
    description: "ScenarioConfig._sweep_altitudes_valid and _sweep_dive_angles_valid do not reject empty lists []. sweep_altitudes_m=[] and sweep_dive_angles_deg=[] pass Pydantic validation silently and propagate to find_worst_trajectories, which returns an empty result list with no error."
    resolution: "Added len(v)==0 guard at the start of both validators, raising ValueError before item-level validation."
    status: resolved
    commit: "0abcf4d"

  - id: D-155
    severity: high
    task: S6.5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 1180
    description: "render_trajectory_map: when all trajectories have identical time_in_detection_s (e.g. all 0.0), det_min==det_max so Normalize(vmin=0, vmax=0) is passed to the colorbar. Matplotlib silently clips to midpoint, making the colorbar labels meaningless."
    resolution: "Guard: if det_max <= det_min, set det_max = det_min + 1.0 so the colorbar always has a meaningful span."
    status: resolved
    commit: "0abcf4d"

  - id: D-156
    severity: medium
    task: S6.5
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 747
    description: "Planning sweep mode: find_worst_trajectories failure is caught and warned but does not set corridor_failed=True. Exit code 0 is returned despite the sweep failing, breaking pipeline error propagation."
    resolution: "Added corridor_failed = True inside the except block so sweep failure propagates to exit code 1."
    status: resolved
    commit: "0abcf4d"

  - id: D-157
    severity: medium
    task: S6.5
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 1215
    description: "render_trajectory_map: DetectionEvent.position_x/y are used in ax.plot() and ax.annotate() without a math.isfinite guard. A NaN/inf from binary-search interpolation would silently produce an invisible annotation."
    resolution: "Added math.isfinite check before ax.plot/annotate; non-finite events emit warnings.warn and are skipped."
    status: resolved
    commit: "0abcf4d"

  - id: D-158
    severity: medium
    task: S6.5
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 602
    description: "sensor_positions is built outside the threat loop and reused, but the variable name shadows an earlier binding at line 495. If the blocks are ever reordered, the wrong position list could be passed to rendering. Code intent is unclear."
    resolution: "Renamed to threat_sensor_positions to eliminate shadowing and clarify intent."
    status: resolved
    commit: "0abcf4d"

  - id: D-159
    severity: medium
    task: S6.5
    found_by: spec_compliance
    file: src/salus/report/maps.py
    line: 988
    description: "render_corridor_polar_diagram extension: spec requires colouring trajectory bars by a secondary dimension (altitude or dive angle). Implementation colours by detection fraction (time_in_detection_s / time_to_asset_s). TrajectoryResult has no altitude/dive-angle field, so full compliance requires model changes beyond S6.5 scope."
    resolution: "Detection fraction (time_in_detection_s / time_to_asset_s) used as secondary dimension — the closest available proxy. Altitude/dive-angle colouring would require adding these fields to TrajectoryResult (out of S6.5 scope). Accepted with stakeholder awareness."
    status: resolved
    commit: "0abcf4d"

  - id: D-160
    severity: high
    task: S6.6
    found_by: silent_failure_hunter
    file: src/salus/engine/path_planner.py
    line: 297
    description: "find_adversarial_trajectory: Dijkstra goal detection early-break fires when heap[0][0] > goal_cost, but multiple altitude-band nodes at the goal (row,col) may still be undequeued and cheaper. The break exits before all goal-band nodes are settled, potentially returning a suboptimal altitude band."
    resolution: "Removed early-break optimisation from Dijkstra goal-detection loop. Goal nodes at all altitude bands are now fully settled before the search terminates. The break was theoretically safe (Dijkstra's invariant guarantees no cheaper node remains once heap min > goal_cost), but the code was simplified to eliminate ambiguity."
    status: resolved
    commit: "68bb78f"

  - id: D-161
    severity: high
    task: S6.6
    found_by: silent_failure_hunter
    file: src/salus/engine/path_planner.py
    line: 359
    description: "find_adversarial_trajectory: waypoint simplification initialises prev_dr=None. At i==1 the condition 'dr != prev_dr' compares an int to None and is always True, unconditionally adding a waypoint at every second node before the first real direction comparison. Result: over-simplified or incorrectly structured waypoint list."
    resolution: "Added 'if prev_dr is None: ... continue' guard for the first interior node (i==1). The first interior node records the baseline direction without emitting a waypoint, so subsequent comparisons are against valid int values, not None."
    status: resolved
    commit: "68bb78f"

  - id: D-162
    severity: high
    task: S6.6
    found_by: silent_failure_hunter
    file: src/salus/engine/path_planner.py
    line: 134
    description: "build_detection_cost_grid: sensor_can_detect_point raises ValueError for both sensor-outside-DEM (configuration error) and target-outside-DEM (expected boundary case). Catching ValueError broadly silently swallows misconfigured sensor placements, making them indistinguishable from normal boundary clipping and hiding operator errors."
    resolution: "Removed ValueError from the broad except clause in build_detection_cost_grid. sensor_can_detect_point returns False (not ValueError) when the target is outside the DEM; ValueError only propagates for sensor-placement configuration errors, which should surface to the caller. Only IndexError (unexpected numpy edge case) is caught."
    status: resolved
    commit: "68bb78f"

  - id: D-163
    severity: medium
    task: S6.6
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 799
    description: "Adversarial origin inference: 'except Exception: worst_results = []' catches and silently discards all errors from find_worst_corridors with no log message. Operators have no visibility that origin inference failed and a fallback was used."
    resolution: "Added 'except Exception as origin_exc:' with a click.echo warning message before the fallback assignment, so operators are notified that corridor-sweep origin inference failed and a site-centre fallback was used."
    status: resolved
    commit: "68bb78f"

  - id: D-164
    severity: medium
    task: S6.6
    found_by: silent_failure_hunter
    file: src/salus/engine/path_planner.py
    line: 336
    description: "find_adversarial_trajectory: when goal_node==-1 (no path found in connected grid), the fallback to a direct two-waypoint trajectory emits no log/warning. Callers have no indication that Dijkstra's search failed to reach the goal."
    resolution: "Added warnings.warn (RuntimeWarning) and _log.warning call in the goal_node==-1 fallback branch, identifying origin/goal coordinates for operator investigation."
    status: resolved
    commit: "68bb78f"

  - id: D-165
    severity: medium
    task: S6.6
    found_by: silent_failure_hunter
    file: src/salus/engine/path_planner.py
    line: 174
    description: "_nearest_node: if cost_grid.altitude_bands_m is empty, min(..., key=...) raises ValueError with an unhelpful 'min() arg is an empty sequence' message. A caller-facing guard with a descriptive error would aid debugging."
    resolution: "Added 'if not bands: raise ValueError(...)' guard at the start of _nearest_node with a descriptive error message identifying the source as an empty altitude_bands_m list."
    status: resolved
    commit: "68bb78f"

  - id: D-166
    severity: medium
    task: S6.6
    found_by: spec_compliance
    file: src/salus/cli.py
    line: 784
    description: "Spec S6.6-3: --adversarial flag has no guard requiring sc.protected_point and sc.threat_profiles. When run without a protected_point or threat definitions, the adversarial block silently falls through to the bare-except handler producing only a warning."
    resolution: "Added an elif branch after the existing 'elif sc.threat_profiles and sc.protected_point is None' block: when --adversarial is set but prerequisites are not met (no threat_profiles or no protected_point), a clear warning message is emitted explaining the skip reason."
    status: resolved
    commit: "68bb78f"

  - id: D-167
    severity: medium
    task: S6.6
    found_by: spec_compliance
    file: tests/test_path_planner.py
    line: 103
    description: "Spec S6.6-1 acceptance criterion 'cells outside sensor detection range must have count 0' has no explicit test. TestDetectionCostGridValues only tests that inside-range cells are nonzero; outside-range cells are not verified."
    resolution: "Added TestCellsOutsideSensorRangeAreZero with two tests: corner cells (~70 m from sensor with max_range_m=30) have count 0; cells ~10 m north of sensor have count >= 1."
    status: resolved
    commit: "68bb78f"

  - id: D-168
    severity: medium
    task: S6.6
    found_by: spec_compliance
    file: tests/test_path_planner.py
    line: 246
    description: "Spec S6.6-2 requires a valley-terrain test showing the adversarial path follows a valley rather than crossing exposed ridges. No valley DEM fixture exists and the test is absent."
    resolution: "Added valley_dem_path fixture to conftest.py (two parallel ridges, central valley floor). Added TestValleyTerrainPathPrefersValley with two tests: valley floor has lower detection than ridge for east-ridge sensor; adversarial path completes on valley DEM."
    status: resolved
    commit: "68bb78f"

  - id: D-169
    severity: medium
    task: S6.6
    found_by: spec_compliance
    file: tests/test_path_planner.py
    line: 246
    description: "Spec S6.6-3 requires an integration test asserting that, on ridge terrain, the adversarial path routes around the ridge rather than over it. The existing ridge test (TestDetectionCostGridRidge) only verifies the grid values, not the resulting trajectory shape."
    resolution: "Added TestRidgeTerrainIntegration with two tests: adversarial path reaches goal on ridge DEM; first waypoint is in the north (low-detection) half of the DEM when sensor is on south side."
    status: resolved
    commit: "68bb78f"

  - id: D-170
    severity: high
    task: S7
    found_by: silent_failure_hunter
    file: src/salus/engine/kill_chain.py
    line: 65
    description: "compute_kill_chain has two independent guards for threat_speed_ms: isinstance+>0 then math.isfinite. float('inf') passes the first guard silently (inf > 0 is True) and only fails the second. If the second guard is removed in a future refactor, inf flows through to 'inf/inf = nan' for available_time_s, causing engagement_feasible to silently return False. The two guards should be merged into one."
    resolution: "Merged the two independent guards into a single `if not (math.isfinite(threat_speed_ms) and threat_speed_ms > 0.0)` check, eliminating the redundant isinstance guard and closing the inf-passthrough gap."
    status: resolved
    commit: "f55b65d"

  - id: D-171
    severity: high
    task: S7
    found_by: silent_failure_hunter
    file: src/salus/engine/kill_chain.py
    line: 82
    description: "_required_time is called twice in the no-detection early-return branch: once for required_time_s and once embedded in margin_s. If the function ever gains side effects or config mutability, the two stored values could diverge, making margin_s != available_time_s - required_time_s in the returned KillChainResult."
    resolution: "Moved `required_time_s = _required_time(kill_chain_config, effector)` above both branches so the same computed value is used in both `required_time_s` and `margin_s` fields, guaranteeing margin_s == available_time_s - required_time_s."
    status: resolved
    commit: "f55b65d"

  - id: D-172
    severity: high
    task: S7
    found_by: silent_failure_hunter
    file: src/salus/report/charts.py
    line: 102
    description: "NaN available_time_s bypasses the > 0.0 filter in max() because NaN comparisons return False. NaN then propagates into margin arithmetic and the chart annotation renders as 'nan' with no error raised. isfinite guard missing in render_kill_chain_chart."
    resolution: "Added `math.isfinite(r.available_time_s)` filter in max() and changed the per-bar guard to `if not math.isfinite(kc.available_time_s) or kc.available_time_s <= 0.0`. Added `if not math.isfinite(margin): _log.warning(...); continue` before margin rendering."
    status: resolved
    commit: "f55b65d"

  - id: D-173
    severity: medium
    task: S7
    found_by: silent_failure_hunter
    file: src/salus/engine/kill_chain.py
    line: 118
    description: "compute_all_kill_chains accepts an empty corridor_results list with no guard. Returns empty list silently with log message '0 corridors evaluated'. Callers that index the result get an undiagnosable IndexError rather than a clear ValueError."
    resolution: "Added `if not corridor_results: raise ValueError(...)` guard at the start of compute_all_kill_chains, mirroring the existing effectors guard."
    status: resolved
    commit: "f55b65d"

  - id: D-174
    severity: medium
    task: S7
    found_by: silent_failure_hunter
    file: src/salus/report/charts.py
    line: 17
    description: "charts.py imports the private function _required_time from salus.engine.kill_chain across module boundaries. If _required_time is renamed or inlined during refactoring, the failure is a runtime ImportError deferred until charts.py is first loaded, not a static error."
    resolution: "Promoted `_required_time` to a public function `required_time(config, effector) -> float` with full docstring. A private alias `_required_time = required_time` preserved for internal callers. Updated charts.py import to use `required_time`."
    status: resolved
    commit: "f55b65d"

  - id: D-175
    severity: medium
    task: S7
    found_by: silent_failure_hunter
    file: src/salus/report/charts.py
    line: 228
    description: "plt.tight_layout() and fig.savefig() are called before plt.close(fig) with no try/finally. If tight_layout() raises (rare but possible with degenerate axis configs), plt.close(fig) is never reached, leaking Matplotlib figure handles."
    resolution: "Wrapped `plt.tight_layout()` and `fig.savefig()` in `try/finally: plt.close(fig)` to ensure figure handle is released even if tight_layout raises."
    status: resolved
    commit: "f55b65d"

  - id: D-176
    severity: medium
    task: S7
    found_by: spec_compliance
    file: src/salus/report/charts.py
    line: 94
    description: "Spec S7-4 names D-T-I-D-E-A phases as coloured segments. The chart renders only T-I-D-E-A (5 segments); the 'Detect' (D) phase has no coloured bar. S7-1 defines no detect_time_s field in KillChainConfig, creating an intra-spec inconsistency. Resolution: document that Detect is the timeline origin (t=0) and not a duration segment."
    resolution: "Added module docstring to charts.py explaining D-T-I-D-E-A phase mapping: Detect (D) is the timeline origin (t=0) with no configurable duration in KillChainConfig and is therefore not rendered as a coloured segment. The T-I-D-E-A phases are rendered as coloured bars."
    status: resolved
    commit: "f55b65d"

  - id: D-177
    severity: high
    task: S9
    found_by: silent_failure_hunter
    file: src/salus/engine/placement.py
    line: 154
    description: "NaN values in DEM nodata cells propagate through np.gradient into gradient_magnitude. NaN > threshold evaluates to False so nodata cells silently pass the slope filter and are included as candidate positions. Sensors can be placed at invalid terrain positions."
    resolution: "Wrapped np.sqrt gradient computation with np.nan_to_num(nan=_STEEP_SLOPE_THRESHOLD + 1.0) so nodata cells map to a value exceeding the threshold and are excluded."
    status: resolved
    commit: "6766887"

  - id: D-178
    severity: high
    task: S9
    found_by: silent_failure_hunter
    file: src/salus/engine/placement.py
    line: 353
    description: "Broad except Exception in candidate-scoring loop logs only at DEBUG level (off in production). If every candidate fails for a sensor, the sensor is silently skipped with no WARNING-level indication. Caller cannot distinguish early-exit from wholesale computation failure."
    resolution: "Added failed_candidates counter; post-loop guard emits WARNING when all candidates raised exceptions, distinguishing total failure from no-improvement skip."
    status: resolved
    commit: "6766887"

  - id: D-179
    severity: high
    task: S9
    found_by: silent_failure_hunter
    file: src/salus/engine/placement.py
    line: 316
    description: "When total_weight == 0.0 (all PlacementWeights are zero and no zones defined), every candidate scores 0.0. Only the first candidate ever wins (0.0 > -1.0), so every sensor is silently placed at the first position rather than the best coverage position. No warning is emitted."
    resolution: "Added early WARNING log when total_weight == 0.0 describing degenerate scoring behaviour and advising to set non-zero weights."
    status: resolved
    commit: "6766887"

  - id: D-180
    severity: medium
    task: S9
    found_by: silent_failure_hunter
    file: src/salus/engine/placement.py
    line: 363
    description: "When best_score == 0.0 (sensor covers only exclusion-zone cells), the sensor is still placed without any warning. Callers that use placement count or coverage percentage to assess quality are misled."
    resolution: "Added post-placement WARNING when best_score <= 0.0 advising user to review zone weights or candidate positions."
    status: resolved
    commit: "6766887"

  - id: D-181
    severity: medium
    task: S9
    found_by: silent_failure_hunter
    file: src/salus/engine/placement.py
    line: 232
    description: "In _build_weight_map, boundary_mask(site, zone.geometry) is called without guarding for invalid/empty Shapely geometries. A failure mid-loop leaves weight_map partially constructed and the traceback contains no zone name context for diagnosis."
    resolution: "Wrapped boundary_mask call in try/except; re-raises as RuntimeError with zone name and type in message for diagnosis."
    status: resolved
    commit: "6766887"

  - id: D-182
    severity: medium
    task: S9
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 0
    description: "In the --write-scenario path, `raw_scenario.get('sensor_placements') or []` only replaces falsy values. A non-empty string or non-list value is trusted and causes `TypeError` on concatenation, which is not caught by the `except OSError` guard."
    resolution: "Replaced `or []` with explicit isinstance(list) check; non-list values default to empty list."
    status: resolved
    commit: "6766887"

  - id: D-183
    severity: medium
    task: S9
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 0
    description: "In the --write-scenario path, yaml.safe_dump is guarded only with `except OSError`. If any SensorPlacement field holds a numpy scalar, yaml.representer.RepresenterError is raised (not an OSError subclass) and propagates unhandled, bypassing the user-friendly error message."
    resolution: "Widened except to (OSError, yaml.YAMLError); serialised_placements now casts all numeric fields via float() to prevent numpy scalar representation errors."
    status: resolved
    commit: "6766887"

  - id: D-184
    severity: high
    task: S8
    found_by: silent_failure_hunter
    file: src/salus/engine/saturation.py
    line: 57
    description: "NaN nodata cells in the DEM propagate silently through _sample_dem_elevation. NaN tgt_z_abs reaches line_of_sight_3d, raises ValueError, caught by except block, returns False — target silently classified as un-engageable. SaturationResult understates capacity with no warning."
    resolution: "Added math.isnan(val) guard in _sample_dem_elevation; logs WARNING with cell coordinates and returns 0.0 rather than propagating NaN."
    status: resolved
    commit: "1a9b26b"

  - id: D-185
    severity: high
    task: S8
    found_by: silent_failure_hunter
    file: src/salus/engine/saturation.py
    line: 53
    description: "_sample_dem_elevation divides by site.resolution with no zero-guard. SiteModel has no resolution validator. resolution=0.0 produces ZeroDivisionError that propagates to CLI's broad except, silently aborting saturation analysis."
    resolution: "Added `if site.resolution <= 0.0: raise ValueError(...)` guard at entry to _sample_dem_elevation before any division."
    status: resolved
    commit: "1a9b26b"

  - id: D-186
    severity: high
    task: S8
    found_by: silent_failure_hunter
    file: src/salus/engine/saturation.py
    line: 297
    description: "approach_distance = max_range * 0.75. Very small max_range_m (e.g. 1e-9) produces approach_distance = 0.0 after floating-point, causing ApproachVector._distance_positive ValueError inside find_saturation_threshold. Not caught; propagates to CLI except, silently aborting sweep."
    resolution: "Added guard in find_saturation_threshold: if approach_distance <= 0 or not finite, log WARNING and return zero-capacity SaturationResult."
    status: resolved
    commit: "1a9b26b"

  - id: D-187
    severity: high
    task: S8
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 992
    description: "When saturation_threshold_n = max_targets + 1 (never reached), threshold_data only contains N=1..20, all with unengaged=0. The axvline at saturation_threshold_n=21 is not drawn. Chart appears identical to a fully-handled scenario — no indicator that threshold was never hit."
    resolution: "Detects threshold_never_reached case; annotates chart title with '(not reached within N=max_n)' and pushes sat_threshold_line off-chart so axvline is not drawn misleadingly."
    status: resolved
    commit: "1a9b26b"

  - id: D-188
    severity: high
    task: S8
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 1042
    description: "sc.protected_point is typed as tuple[float,float]|None. The outer guard ensures non-None at this call site, but there is no type assertion. A future refactor removing the guard would cause TypeError inside _target_crs_position with no callsite context."
    resolution: "Added `assert sc.protected_point is not None, '...'` immediately before allocate_effectors call to narrow type and provide explicit diagnostic message."
    status: resolved
    commit: "1a9b26b"

  - id: D-189
    severity: medium
    task: S8
    found_by: silent_failure_hunter
    file: src/salus/engine/saturation.py
    line: 118
    description: "_effector_can_engage catches only ValueError from line_of_sight_3d. Other exceptions (IndexError, MemoryError) propagate uncaught to CLI, where the target is silently dropped. Warning log does not include exception type."
    resolution: "Widened `except ValueError` to `except Exception as los_exc` with `# noqa: BLE001`; warning log now includes `type(los_exc).__name__` for diagnostics."
    status: resolved
    commit: "1a9b26b"

  - id: D-190
    severity: medium
    task: S8
    found_by: silent_failure_hunter
    file: src/salus/engine/saturation.py
    line: 425
    description: "compute_reengagement_timeline: when no placement passes active_names filter, per_effector_engagements is empty and min_cycle_time = math.inf. Guard resets it to window_s (60.0). ReengagementResult.reengagement_cycle_time_s returns 60.0 masquerading as a real measurement."
    resolution: "Changed fallback from window_s to 0.0 so callers can distinguish 'no active effectors' from a real measurement; also logs WARNING."
    status: resolved
    commit: "1a9b26b"

  - id: D-191
    severity: medium
    task: S8
    found_by: silent_failure_hunter
    file: src/salus/engine/saturation.py
    line: 466
    description: "_BEARING_MAX_DEG declared twice — once used at line 297 and re-exported at line 466. Duplicate private constant: future divergence would silently produce wrong bearing distributions. Remove redundant re-declaration."
    resolution: "Removed the duplicate re-declaration at end of file. The single authoritative definition at the top of the constants block is retained with an explanatory comment."
    status: resolved
    commit: "1a9b26b"

  - id: D-192
    severity: medium
    task: S8
    found_by: silent_failure_hunter
    file: src/salus/report/charts.py
    line: 474
    description: "render_engagement_timeline_chart: when reaction_s > window_s, while loop never executes, idle bar drawn with no firing events. No warning emitted — chart consumer cannot distinguish 'effector too slow' from data error."
    resolution: "Added `fired` boolean flag in loop; post-loop `if not fired: _log.warning(...)` emits WARNING with effector name, reaction_time, and window_s."
    status: resolved
    commit: "1a9b26b"

  - id: D-193
    severity: high
    task: S10
    found_by: silent_failure_hunter
    file: src/salus/report/charts.py
    line: 690
    description: "render_coverage_comparison_chart: per_zone_coverage_pct_a.get(z, 0.0) silently substitutes 0.0 for a missing zone key. If a manually-constructed ComparisonResult has a zone in per_zone_delta_pct absent from per_zone_coverage_pct_a, the delta annotation on that bar is computed against 0.0 rather than the true A value — wrong output with no error."
    resolution: ""
    status: open
    commit: ""

  - id: D-194
    severity: high
    task: S10
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 1655
    description: "_load_site_for_comparison has no exception handling. Failure in load_scenario or load_dem is swallowed by a catch-all at the render call site; the compare command exits 0 even though no delta map was produced, with only a one-line warning."
    resolution: ""
    status: open
    commit: ""

  - id: D-195
    severity: high
    task: S10
    found_by: silent_failure_hunter
    file: src/salus/engine/comparison.py
    line: 183
    description: "compare_configs: no bounds/NaN check on total_coverage_pct before arithmetic. If upstream produces NaN or out-of-range coverage, coverage_delta_pct silently becomes NaN and flows into ComparisonResult and CLI output with no error raised."
    resolution: ""
    status: open
    commit: ""

  - id: D-196
    severity: medium
    task: S10
    found_by: silent_failure_hunter
    file: src/salus/engine/comparison.py
    line: 173
    description: "compare_configs: NaN cells in a float composite cast silently to True by .astype(bool), causing those cells to appear covered in the delta grid. Wrong cell-level output with no warning."
    resolution: ""
    status: open
    commit: ""

  - id: D-197
    severity: medium
    task: S10
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 1607
    description: "render_delta_map: _hillshade ValueError for NaN DEM surfaces only as a generic CLI warning; the diagnostic (DEM path, NaN count) is lost and the output directory is silently incomplete."
    resolution: ""
    status: open
    commit: ""

  - id: D-198
    severity: medium
    task: S10
    found_by: silent_failure_hunter
    file: src/salus/engine/comparison.py
    line: 52
    description: "ConfigurationResult: no shape-consistency check between composite and stats.redundancy_map. A caller can pair a populated composite with the zero-shape default redundancy_map, producing shape mismatches that only surface as confusing index errors downstream."
    resolution: ""
    status: open
    commit: ""

  - id: D-199
    severity: medium
    task: S10
    found_by: spec_compliance
    file: src/salus/report/maps.py
    line: 1552
    description: "render_delta_map is missing its '-> Path' return type annotation, violating the project-wide type-annotation rule (CLAUDE.md). mypy infers '-> None'."
    resolution: ""
    status: open
    commit: ""

  - id: D-200
    severity: medium
    task: S10
    found_by: type_design_analyzer
    file: src/salus/engine/comparison.py
    line: 74
    description: "ConfigurationResult.composite has no runtime ndim guard. compare_configs checks .size == 0 but not .ndim == 2. A 1-D array of non-zero length would pass the guard and silently produce a malformed delta_grid."
    resolution: ""
    status: open
    commit: ""

  - id: D-201
    severity: high
    task: S11-1
    found_by: silent_failure_hunter
    file: src/salus/engine/effector_coverage.py
    line: 198
    description: "Non-LOS path: if placement.position_x or position_y is non-finite (e.g. NaN), np.sqrt produces NaN, range_mask becomes all-False, and the engagement zone is silently all-False with no error raised."
    resolution: "Added math.isfinite guard on position_x and position_y at top of compute_single_effector_coverage; raises ValueError with coordinates."
    status: resolved
    commit: "a759806"

  - id: D-202
    severity: high
    task: S11-1
    found_by: silent_failure_hunter
    file: src/salus/engine/effector_coverage.py
    line: 191
    description: "No site.resolution <= 0 guard in _clip_to_effector. If resolution=0, coordinate grids collapse to a single point (all cells mapped to origin), producing a silently wrong uniform-distance range_mask."
    resolution: "Added resolution guard at top of _clip_to_effector: raises ValueError if not finite or <= 0, consistent with viewshed.clip_viewshed_to_sensor."
    status: resolved
    commit: "a759806"

  - id: D-203
    severity: medium
    task: S11-1
    found_by: silent_failure_hunter
    file: src/salus/engine/effector_coverage.py
    line: 121
    description: "compute_effector_layer_coverage loop has no per-effector exception handler. A GDAL RuntimeError from compute_viewshed aborts the entire layer with no log identifying which effector caused the failure."
    resolution: "Wrapped per-effector call in try/except Exception; re-raises as RuntimeError naming the effector and its position."
    status: resolved
    commit: "a759806"

  - id: D-204
    severity: medium
    task: S11-1
    found_by: silent_failure_hunter
    file: src/salus/engine/effector_coverage.py
    line: 119
    description: "Empty effector_pairs list returns all-False array silently. A configuration error (effector list not populated) would not be distinguishable from a valid zero-effector scenario without a warning log."
    resolution: "Added _log.warning call when effector_pairs is empty before the union loop."
    status: resolved
    commit: "a759806"

  - id: D-205
    severity: medium
    task: S11-1
    found_by: type_design_analyzer
    file: src/salus/engine/effector_coverage.py
    line: 69
    description: "Local variable effector_agl lacks explicit float annotation; intermediate arrays in _clip_to_effector (col_coords, row_coords, cell_x, cell_y, dx, dy, dist) lack annotations. Project rule: type-annotate everything."
    resolution: "Added explicit type annotations for effector_agl and all intermediate NDArray variables in _clip_to_effector."
    status: resolved
    commit: "a759806"
