# Salus Defect Register
# Auto-maintained — do not edit manually during active task execution
# Format: see CLAUDE.md → Defect Register
#
# Severity: critical | high | medium | low
# Status:   open | resolved
#
# Log findings here BEFORE fixing them.
# Update status + commit after fixing.

defects:
  - id: D-001
    severity: medium
    task: baseline
    found_by: G4_mypy
    file: src/salus/report/maps.py
    line: 49
    description: "extent passed as list[float] to imshow; expects tuple[float, float, float, float] | None"
    resolution: "Changed list literal to tuple literal"
    status: resolved
    commit: ""  # Will be set on next commit

  - id: D-002
    severity: medium
    task: baseline
    found_by: G7_coverage
    file: src/salus/report/maps.py
    line: null
    description: "maps.py has no tests — total coverage at 61%, below 80% threshold"
    resolution: "Added tests/test_maps.py with 12 tests; coverage now 81.4%"
    status: resolved
    commit: ""

  - id: D-003
    severity: high
    task: S1-3
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 72
    description: "ScaleBar try/except block uses 'except Exception: pass' — import is at module level so ImportError is already handled; block silently swallows real runtime errors (bad axes state, matplotlib version mismatch) with no diagnostic"
    resolution: "Moved ScaleBar import inside the try block (catches ImportError); separated into ImportError (silent skip) and Exception (warnings.warn) branches"
    status: resolved
    commit: ""

  - id: D-004
    severity: high
    task: S1-3
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 97
    description: "plt.close(fig) is not in a try/finally block — if fig.savefig() raises (missing parent dir, disk full, permission denied), the figure handle is leaked"
    resolution: "Wrapped fig.savefig in try/finally to guarantee plt.close(fig) is always called"
    status: resolved
    commit: ""

  - id: D-005
    severity: high
    task: S1-3
    found_by: silent_failure_hunter
    file: src/salus/report/maps.py
    line: 15
    description: "np.gradient propagates NaN when DEM contains nodata cells; NaN flows through arctan/clip into imshow silently, rendering as transparent without any warning to the caller"
    resolution: "Added NaN guard at top of _hillshade raising ValueError with clear message"
    status: resolved
    commit: ""

  - id: D-006
    severity: medium
    task: S1-3
    found_by: silent_failure_hunter
    file: src/salus/ingest/terrain.py
    line: 37
    description: "EPSG parsing failure caught by 'except Exception: pass' sets crs_epsg to None silently — downstream code depending on CRS operates without one with no diagnostic"
    resolution: "Added warnings.warn with CRS and exception detail so callers know CRS is unavailable"
    status: resolved
    commit: ""

  - id: D-007
    severity: medium
    task: S1-3
    found_by: silent_failure_hunter
    file: tests/test_maps.py
    line: 130
    description: "isinstance check uses 'type(pytest.importorskip(\"pathlib\").Path())' — semantically misleading, importorskip is a skip-test mechanism not a type accessor; pathlib is stdlib and always available"
    resolution: "Replaced with 'from pathlib import Path' import and direct isinstance(result, Path) check"
    status: resolved
    commit: ""

  - id: D-008
    severity: medium
    task: S1-3
    found_by: silent_failure_hunter
    file: tests/test_maps.py
    line: 65
    description: "test_no_coverage_still_renders only asserts result.exists() — does not verify the output is a valid non-corrupt PNG"
    resolution: "Added Image.open and format/size assertions to the test"
    status: resolved
    commit: ""

  - id: D-009
    severity: high
    task: S1-4
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 63
    description: "compute_viewshed() called with no exception handling — any internal error produces a raw traceback; process exit code is unreliable"
    resolution: "Wrapped in try/except Exception; emits click.echo error and sys.exit(1)"
    status: resolved
    commit: ""

  - id: D-010
    severity: high
    task: S1-4
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 68
    description: "render_coverage_map() called with no exception handling — disk full / permission errors produce raw traceback; partial output file not cleaned up"
    resolution: "Wrapped in try/except Exception; partial output file deleted on failure; sys.exit(1)"
    status: resolved
    commit: ""

  - id: D-011
    severity: medium
    task: S1-4
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 50
    description: "load_dem() only catches FileNotFoundError — PermissionError, RasterioIOError, and corrupt file errors produce raw tracebacks"
    resolution: "Added broad except Exception clause after FileNotFoundError with clean error message"
    status: resolved
    commit: ""

  - id: D-012
    severity: medium
    task: S1-4
    found_by: silent_failure_hunter
    file: src/salus/cli.py
    line: 66
    description: "coverage.size == 0 not guarded — degenerate DEM would produce ZeroDivisionError or NaN printed silently as 'nan% visible'"
    resolution: "Added coverage.size == 0 guard with error message and sys.exit(1)"
    status: resolved
    commit: ""

  - id: D-013
    severity: high
    task: S1-4
    found_by: integration_test
    file: src/salus/engine/viewshed.py
    line: 80
    description: "GDAL maxDistance sentinel is -1.0 when max_range is None — GDAL interprets negative distance as zero-range, returning all cells invisible (0.0% visible). Correct sentinel is 0.0 (unlimited)."
    resolution: "Changed sentinel from -1.0 to 0.0; added 'is not None' check to avoid falsy trap on max_range=0.0"
    status: resolved
    commit: ""

  - id: D-014
    severity: high
    task: D-013-review
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 99
    description: "vs_ds not None-guarded after gdal.ViewshedGenerate() — function can return None on failure even with UseExceptions() active in some GDAL builds, causing AttributeError with no diagnostic context"
    resolution: "Added explicit None guard after ViewshedGenerate; raises RuntimeError with diagnostic message; ds closed before raising"
    status: resolved
    commit: ""

  - id: D-015
    severity: medium
    task: D-013-review
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 99
    description: "No try/finally around ReadAsArray/cleanup — if ReadAsArray or astype raises, GDAL in-memory datasets are never explicitly closed"
    resolution: "Wrapped ReadAsArray/astype in try/finally; vs_ds and ds set to None in finally block to release GDAL handles"
    status: resolved
    commit: ""

  - id: D-016
    severity: medium
    task: D-013-review
    found_by: silent_failure_hunter
    file: src/salus/engine/viewshed.py
    line: 63
    description: "driver.Create() return value not None-guarded — returns None if MEM driver unavailable or rows/cols are zero, causing AttributeError on next line with no context"
    resolution: "Added None guard after driver.Create(); raises RuntimeError with raster dimensions in message"
    status: resolved
    commit: ""

  - id: D-017
    severity: medium
    task: D-013-review
    found_by: regression_reviewer
    file: src/salus/engine/viewshed.py
    line: 80
    description: "max_range=0.0 maps to GDAL unlimited (correct) but NumPy path treats it as 1-cell range (if max_range is not None is True for 0.0) — GDAL/NumPy semantic divergence for this edge case"
    resolution: "Added normalisation at compute_viewshed entry: max_range <= 0.0 converted to None so both paths treat it as unlimited; tests added for max_range=0.0 and max_range<0.0"
    status: resolved
    commit: ""

  - id: D-018
    severity: high
    task: S2-1
    found_by: silent_failure_hunter
    file: src/salus/models/sensor.py
    line: 52
    description: "frequency_bands: list[str] = [] uses bare mutable default — Pydantic v2 copies it safely but the pattern is misleading and not idiomatic; Field(default_factory=list) is the correct form"
    resolution: "Changed to Field(default_factory=list)"
    status: resolved
    commit: ""

  - id: D-019
    severity: medium
    task: S2-1
    found_by: silent_failure_hunter
    file: src/salus/models/sensor.py
    line: 34
    description: "name (SensorDefinition line 34, EffectorDefinition line 121) and defeat_mechanism (line 151) accept empty or whitespace-only strings — produces blank labels in downstream reports without error"
    resolution: "Added _name_non_empty validator to both models; added _defeat_mechanism_non_empty to EffectorDefinition; tests added"
    status: resolved
    commit: ""

  - id: D-020
    severity: medium
    task: S2-1
    found_by: silent_failure_hunter
    file: src/salus/models/sensor.py
    line: 136
    description: "reaction_time_s accepts 0.0 — any downstream 1/reaction_time_s throughput calculation would produce ZeroDivisionError or inf silently; must be strictly positive"
    resolution: "Changed validator bound from >= 0 to > 0; test added for reaction_time_s=0.0 rejection"
    status: resolved
    commit: ""

  - id: D-021
    severity: medium
    task: S2-1
    found_by: type_design_analyzer
    file: src/salus/models/sensor.py
    line: 108
    description: "model_validator return type annotated as concrete class (SensorDefinition/EffectorDefinition) instead of Self — breaks subclass inference and creates forward-reference fragility"
    resolution: "Imported Self from typing; changed return annotations to -> Self on both _min_less_than_max validators"
    status: resolved
    commit: ""

  - id: D-022
    severity: medium
    task: S2-1
    found_by: type_design_analyzer
    file: src/salus/models/sensor.py
    line: 10
    description: "SensorType and EffectorType use (str, Enum) multiple inheritance — str(SensorType.RF) returns 'SensorType.RF' not 'RF'; StrEnum (Python 3.11+) is the correct pattern and serialises cleanly"
    resolution: "Changed both enums to inherit from StrEnum (Python 3.11+); removed str, Enum multiple inheritance"
    status: resolved
    commit: ""

  - id: D-023
    severity: low
    task: S2-1
    found_by: silent_failure_hunter
    file: src/salus/models/sensor.py
    line: 102
    description: "if v is not None guard in _cost_non_negative flagged as dead code — confirmed NOT dead: Pydantic v2 does call field validators with None for float | None fields when None is explicitly passed"
    resolution: "Guard retained; D-023 finding was a false positive from the review agent — verified by failing test without guard"
    status: resolved
    commit: ""

  - id: D-024
    severity: high
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 29
    description: "path.open() in _load_yaml_records only catches yaml.YAMLError — OSError/PermissionError escape without path context and violate documented ValueError-only contract"
    resolution: "Added except OSError block that re-raises with path context; OSError documented in docstring"
    status: resolved
    commit: ""

  - id: D-025
    severity: high
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 38
    description: "Return type list[dict] is inaccurate when YAML top-level is a list — raw list returned without element validation, making isinstance guard in caller appear redundant and removable"
    resolution: "Element validation moved into _load_yaml_records; return type changed to list[dict[str, Any]]; isinstance guard removed from caller; test added"
    status: resolved
    commit: ""

  - id: D-026
    severity: medium
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 71
    description: "directory.iterdir() can raise PermissionError — not caught, propagates with no path context and is not in documented exception contract"
    resolution: "Wrapped iterdir() in try/except PermissionError; re-raised with directory path and label in message; documented in docstring"
    status: resolved
    commit: ""

  - id: D-027
    severity: medium
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 73
    description: "Empty directory returns [] after warnings.warn — docstrings of load_sensors/load_effectors do not document that empty list is a possible return; silent empty-list downstream causes zero-coverage simulation"
    resolution: "Added explicit empty-list warning to Returns section of load_sensors and load_effectors docstrings"
    status: resolved
    commit: ""

  - id: D-028
    severity: low
    task: S2-2
    found_by: silent_failure_hunter
    file: src/salus/ingest/sensors.py
    line: 19
    description: "Return type annotation list[dict] on _load_yaml_records misleads static analysis — the list from yaml.safe_load may contain non-dict elements before element validation runs"
    resolution: "Return type changed to list[dict[str, Any]]; element validation now inside _load_yaml_records so annotation is accurate"
    status: resolved
    commit: ""

  - id: D-029
    severity: medium
    task: S2-2
    found_by: type_design_analyzer
    file: src/salus/ingest/sensors.py
    line: 14
    description: "T = TypeVar('T') is unconstrained — should be bound=BaseModel to reject non-Pydantic classes at the call site and make the contract explicit"
    resolution: "Changed to TypeVar('T', bound=BaseModel); added BaseModel import"
    status: resolved
    commit: ""

  - id: D-030
    severity: low
    task: S2-2
    found_by: type_design_analyzer
    file: src/salus/ingest/sensors.py
    line: 104
    description: "model_cls(**record) is unverifiable by mypy — reliance on Pydantic ValidationError for runtime type guarantee is correct but undocumented in code"
    resolution: "Added inline comment explaining the deliberate reliance on Pydantic's runtime ValidationError"
    status: resolved
    commit: ""
